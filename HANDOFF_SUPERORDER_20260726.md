# 沪市集合竞价 + 超级盘口调研交接（2026-07-26 会话）

> 接续 `HANDOFF_PUSH_INVESTIGATION_20260724.md` 的「集合竞价协议破解」章节。
> 本会话目标：完善沪市竞价解析 + 突破量/未匹配量字段。

---

> ## ★★★ 2026-07-26 22:00 重大更新（冷启动抓包，推翻多个旧结论）★★★
>
> 用户做了**冷启动全流程抓包**（`superorder_20260726_213855.pcap`，8 条 8901 流，2MB），
> 配合 12 个价/量/未匹配锚点（9:15:03~9:24:57），得出以下**确定性结论**：
>
> ### ✅ 已彻底破解
> 1. **时间戳是连续的！旧文档「缺 33 条 / 服务器只给到 9:22:39」是错的。**
>    - 真相：9:15:00~9:25:00 完整覆盖。**⚠️ 22:00 版本写的"200/201 条"基于 2 字节扫描有 4 个假阳性，已修正——见下方「23:30 深度复核」**
>    - 旧扫描漏秒，是因为只认 4 字节 `[b0,b1,0x62,0x6a]`，但部分 tick 时间戳第 3/4 字节是变体
>    - **正确解码（23:30 修正版）**：`ts = 0x6a620000 + b1*256 + b0`，b1∈{0xbc,0xbd,0xbe}；识别规则 = `[b0,b1∈{bc,bd,be}]` 且**后续 1-2 字节内出现 0x62**（`0x62` 恒在，只是位置在 byte2 或 byte3）→ 实测 **199/201** 条，仅缺 9:16:33/36/48（b1 进位边界 bc↔bd）
> 2. **字段表来源已定位**：在响应帧的 `hd1.0` 标记之后，不在 init 握手帧里。
>    - 竞价响应（DT=7176）字段表 4 字段：dt=10(价)/49(量)/27(未匹配)/33，**与请求 `DataType=10,27,33,49` 一一对应**
> 3. **量编码确认**：**LE24 固定 3 字节，单位股**。12 个锚点全部精确匹配（含零股：3440828 股 = 34408.28 手 ✓）
> 4. **未匹配编码确认**：变长 LE（2-3 字节），单位股，紧大量字段之后
>
> ### ❌ 仍未破解（核心难点）
> **字段在记录里的起始位置漂移**——量在 +1/+2/+4/+5/+7 都出现过。试过的所有确定性规则都只对部分 tick 成立：
> - 「找 b0」：只对价变化的 tick 有效（约 12/164）
> - 「+0 字节 bit7=1 → 标记 1 字节」：只对 9:15:36~48 有效
> - 根因：字段表 fmt 标记（dt49=`[22,70]`、dt33=`[34,70]` 比 dt10/27 的 `[70]` 多一个子标记）的**子标记语义未破译**
> - **不能用启发式（合理范围/单调性）**——客户端是确定性解析，规则必然在 fmt 标记里
>
> ### 📁 新增关键文件
> - `captures_live/superorder_20260726_213855.pcap` — **冷启动抓包**（8901 全流程）
> - `captures_live/superorder_20260726_213855_resp_stream0.bin` — 603118 响应流（599KB，含字段表）
>   - 竞价响应帧：off=488747（2654 字节），字段表在帧+52，9:15:00 时间戳在帧+81

---

## 一、本会话脉络（按时间顺序）

1. **沪市竞价变长格式破解（价格字段）** — 实现 `_parse_auction_sh`，用 3 字节时间戳扫描拿到 167 条
2. **多票对比脚本** — 写 `tests/test_auction_sh_compare.py`，发现 600276/601318 价格解析异常
3. **价格锚点法的局限暴露** — 不同股价的票价格范围不同，量/未匹配量完全解不出
4. **抓超级盘口** — 写 `tests/capture_superorder.py`，用户抓到 `superorder_20260726_201429.pcap`
5. **★ 定位超级盘口协议** — pageid=4260 + 周期码 7169，含完整字段表（29 字段）
6. **★ 时间戳破解（9:15-9:25 完整 200 条）** — 推翻「缺 33 条」旧结论（详见更新摘要）
7. **★★ 冷启动抓包定位字段表** — 字段表在响应帧 `hd1.0` 后，与请求 DataType 对应
8. **量/未匹配编码破解（12 锚点验证）** — 量=LE24，未匹配=变长LE，单位均为股
9. **字段起始位置漂移未解** — fmt 子标记（22/34）语义待破译

---

## 二、★ 核心发现：超级盘口是全新协议

用户反馈「同花顺超级盘口能看到 9:15-9:25 每 3 秒一条、约 200 条完整数据」。抓包发现这走的是**完全不同的协议**，不是之前的 4214/7176。

### 协议对比

| 维度 | 分时/竞价（之前已实现） | **超级盘口（本会话新发现）** |
|---|---|---|
| pageid | 4214 | **4260** |
| 周期码 | 7176（竞价）/ 8192（分时）| **7169**（逐笔回放）|
| DataType | 10,27,33,49 | **10,12,13** |
| 响应大小 | ~2.6KB（单帧）| **440KB+**（含逐笔明细）|
| 字段数 | 5 个（dt1/10/49/27/33）| **29 个**（dt10/13/12/20/21/49/18/19/74/25-29/31-35/14/150-153/103-112）|
| 时间覆盖 | 9:15-9:22:39（缺秒）| **9:15-15:00 全天逐笔**（每 3 秒一条）|
| 时间戳密度 | 167 条（有跳秒）| **接近 200 条完整**（最长连续段 197/217）|

### 7169 请求特征（抓包确认）

```
CodeList=17(603118,);
DataType=10,12,13,
DateTime=7169(<start_unix>-<end_unix>)
LackTime=0,0,0,0,0,0,0,0
pageid=4260
```

- **DateTime 参数是 unix 时间戳区间**：用户在超级盘口里滚动到哪个时段，就请求哪段（如 `7169(1784855700-1784856300)` = 9:15-9:25，`7169(1784857554-1784858154)` = 9:19-9:20）
- hexin 客户端打开超级盘口时会**连续发多个 7169 请求**覆盖不同时段（抓包里 71 个 7169 请求，每个请求一段几十秒到几分钟的数据）
- DataType=10（价）/12（？）/13（？），响应实际返回 29 个字段

### 4260 响应帧结构（440KB 逐笔帧，已部分破解）

```
帧头: MarketTime=16(...);272(...);528(...)  + ServerCost=514
     ↓
"hd\x8d1.0" 标记（注意：是 hd + 0x8d + "1.0"，不是标准 hd1.0\0）
     ↓
字段表（173 字节，29 个字段，变长编码）
     ↓
0x11 + "603118" 个股壳（标记字节 0x11 = 沪市）
     ↓
逐笔记录区（每条 = 4 字节 unix 时间戳 + 29 个变长字段）
```

### ★ 字段表格式（已提取，关键字段边界仍需破解）

字段表区（dt10 起，off **115**；off 98 为 `hd` 标记，off 104-114 为填充/校验区），按 `width=04` 切分得到 29 个字段条目。**每条 = dt + fmt标记 + width**，fmt 标记变长（有时 `70`，有时 `70 88`，有时 `11 70` 等）：

```
[ 0] dt=10  (价)     entry=0a 70 04
[ 1] dt=13           entry=0d 70 88 04
[ 2] dt=12           entry=0c 30 04
[ 3] dt=20           entry=14 70 88 04
[ 4] dt=21           entry=15 70 04
[ 5] dt=49  (量)     entry=31 70 88 04
[ 6] dt=18           entry=12 70 04
[ 7] dt=19           entry=13 70 99 04
[ 8] dt=74           entry=4a 70 4b 70 91 04
[ 9] dt=25           entry=19 70 04
[10] dt=26           entry=1a 11 70 04
[11] dt=27  (未匹配) entry=1b 70 04
[12] dt=28           entry=1c 11 70 04
[13] dt=29           entry=1d 70 04
[14] dt=31           entry=1f 11 70 04
[15] dt=32           entry=20 70 04
[16] dt=33           entry=21 11 70 04
[17] dt=34           entry=22 70 04
[18] dt=35           entry=23 11 70 04
[19] dt=14           entry=0e 70 04
[20] dt=150          entry=96 11 70 04
[21] dt=151          entry=97 70 04
[22] dt=152          entry=98 13 70 04
[23] dt=153          entry=99 70 36 9a 70 6c 9b 70 d9 9c 70 9d 70 b3 66 70 22 04  ← 切分异常，含多个dt
[24-28] ...          (尾部切分不准，但前面 dt0-22 清晰)
```

**fmt 标记变长规律**（待确认）：`70`=普通 ths_float / `30`=整数 / `88`/`99`/`91`/`11` 可能是 scale 或 flags。这些变长标记**很可能就是字段边界的关键** —— 沪市逐笔用变长存储，每个字段的字节数由 fmt 标记决定。

### 逐笔记录的字段数据（9:15:03 锚点对照）

9:15:03 锚点（用户提供）：**价 15.01 / 量 73600 / 未匹配 700 / 额 1104700**

帧内 9:15:03 记录（off **376** 起；旧笔记作 364，实测差 12 字节，已修正）：
```
97 bc 62 6a | a2 3a 1d b0 00 00 00 ff 6c a2 3a d9 a2 3a 80 1f 33 01 00 00 e2 b5 ff ff 5c 80 1f 01 36 bc 02 7c 00 00 1f 80 80 1f 01 14 00 7d 80 00 5f ff b1 67 a7 3f b0 68 3c 01 98 78 ...
└─时间戳──┘ └──────────── 29 个变长字段 ────────────────────────────────────┘
            a2 3a = 0x3aa2 = 15010 → 15.01 ✓ (价 dt10)
            1d b0 = 价的 exp 部分（1d 是中间字节，与 7176 查询的 33 不同）
            ff ff ff ff = 哨兵（某字段无效值）
            80 1f ... = 量相关（73600 = 0x00011f80）
            bc 02 ... = 未匹配相关（700 = 0x000002bc）
```

**未解的核心难点**：29 个字段如何在字节流里切分。已知 dt10（价）的尾数是 `a2 3a`（LE16），但完整的 dt10 占几字节？后续字段如何衔接？字段表的 fmt 标记（`70`/`88`/`99`/`11`）决定每个字段几字节 —— **这是下一步要破解的**。

---

## 三、沪市竞价（4214/7176）现状 — 已实现但有局限

代码位置：`src/thspypc/protocol.py:_parse_auction_sh`

### 已实现（旧逻辑，待用新规则重写）

- **时间戳**：3 字节压缩扫描 `[b0, b1∈0xbc..0xbe, 0x62]` + 固定 byte3=0x6a，按 ts 排序去重 → **167 条**
- **价格**：找 `0xb0` 标记（ths_float exp=3 除 1000），用「前价偏差<20%」选候选 → 603118 全程 15.01~16.43 正确
- 实跑验证：`tests/test_auction.py 603118` 拿到 167 条

### ⚠️ 旧逻辑已被推翻（2026-07-26 22:00 修正）

1. ~~**缺秒**：应有 200 条，实际 167 条，缺 33 条~~ → **时间戳实际是完整的 199/201 条**（23:30 复核修正，见九章 9.1），旧扫描漏了变体时间戳（b2≠0x62）。正确解码见下方「时间戳编码（最终版）」
2. ~~**服务器只给到 9:22:39**~~ → **错误**。服务器返回完整的 9:15:00~9:25:00 全部 tick，旧解析漏了尾部 596 字节的数据
3. **价格靠猜**：用「前价偏差<20%」是治标。新发现价字段紧跟 ts 后（有 b0 标记时），可确定性定位
4. **量/未匹配已破译编码**（LE24 / 变长 LE，单位股），**但字段起始位置漂移未解**

### 为什么 4214/7176 仍是正解（修正旧结论）

~~旧文档说「4214/7176 是竞价摘要、只给到 9:22:39，应转向 4260/7169」。这是错的。~~
**4214/7176 返回的就是完整的竞价逐笔**（9:15:00~9:25:00，200 条 tick，含价/量/未匹配）。冷启动抓包已确认：请求 `DT=7176(1784855700-1784856300) DataType=10,27,33,49` 的响应帧含完整字段表 + 全部 tick。**应继续攻 4214/7176，不要转向 4260/7169**（4260 是盘中逐笔，字段更多更复杂）。

---

## 四、★ 字段表已定位（冷启动抓包突破）

### 字段表位置

字段表**在响应帧的 `hd1.0` 标记之后**，不在 init 握手帧里。响应帧结构：

```
[fdfd分隔] [帧头文本(MarketTime/ServerCost)] [路由标记 0xfc01竞价] [hd1.0标记] [字段表] [个股壳 0x11+code] [tick数据]
```

竞价响应帧（`superorder_20260726_213855_resp_stream0.bin` off=488747，2654 字节）实测：
- 帧+36：`hd 93 31 2e 30`（hd1.0 变体）
- **帧+52：字段表起点**（按 `0x04` 切分）
- 帧+69：个股壳 `11 36 30 33 31 31 38`（0x11 + "603118"）
- 帧+81：第一条 tick `94 bc 62 6a`（9:15:00）

### 字段表格式（实测）

字段表 = 若干条目，每条 = `[dt] [fmt标记...] 0x04`。竞价响应（DT=7176）的字段表 4 条：

```
[0] dt=10  (价)     fmt=[0x70]           raw=0a 70 04
[1] dt=49 (量)     fmt=[0x22,0x70]      raw=31 22 70 04    ← 22 是量的子标记
[2] dt=27 (未匹配)  fmt=[0x70]           raw=1b 70 04
[3] dt=33          fmt=[0x34,0x70]      raw=21 34 70 04    ← 34 是 dt33 的子标记
```

**字段表顺序与请求 `DataType=10,27,33,49` 对应**（注意响应里顺序是 10,49,27,33，可能按某种规则重排）。

### fmt 标记语义（部分破解，核心难点）

- `0x70`：主类型标记（所有字段都有），可能 = 「LE 变长数值」
- **`0x22`**：dt49(量) 的子标记，**很可能编码量的字段宽度规则** —— 语义未破译
- **`0x34`**：dt33 的子标记 —— 语义未破译
- dt10(价) 和 dt27(未匹配) 只有 `[0x70]`，无子标记

**字段起始位置漂移的根因就在这里**：客户端靠 fmt 子标记（22/34）知道每个字段几字节、怎么切分。子标记语义未解 → 无法确定性切分。

### 12 个锚点验证的字段编码（单位均为「股」，显示时 ÷100 到手）

| 时刻 | 价 | 量(股) | 量字节 | 未匹配(股) | 未匹配字节 |
|------|-----|--------|--------|-----------|-----------|
| 9:15:03 | 15.01 | 73600 | `80 1f 01` | 700 | `bc 02` |
| 9:15:15 | 15.47 | 425400 | `b8 7d 06` | 11800 | `18 2e` |
| 9:16:54 | 15.35 | 704400 | `90 bf 0a` | 61300 | `74 ef` |
| 9:17:03 | 15.45 | 660600 | `78 14 0a` | 16200 | `48 3f` |
| 9:17:09 | 15.43 | 670800 | `50 3c 0a` | 5600 | `e0 15` |
| 9:20:09 | — | 786100 | `b4 fe 0b` | 225100 | `4c 6f 03` |
| 9:24:57 | 15.40 | 3440828 | `bc 80 34` | 59172 | `24 e7` |
| 9:15:36 | 15.43 | 562100 | `b4 93 08` | 5500 | `7c 15` |
| 9:15:39 | 15.45 | 555800 | `18 7b 08` | 1800 | `08 07` |
| 9:15:42 | 15.43 | 560100 | `e4 8b 08` | 3300 | `e4 0c` |
| 9:15:45 | 15.43 | 562000 | `50 93 08` | 1100 | `4c 04` |

**结论**：
- **量 = LE24 固定 3 字节**（所有锚点都是 3 字节，连零股都对：3440828 股 = 34408.28 手）
- **未匹配 = 变长 LE**（去高位 0）：700/11800 用 2 字节，225100 用 3 字节
- **未匹配紧大量字段之后**（4 锚点验证：量结束 → 未匹配开始，无间隔或隔 1 字节标记）
- **价 = LE16 ÷ 1000**（仅价变化时出现，增量编码）

### 字段起始位置漂移（未解的核心）

> ⚠️ 2026-07-26 23:30 复核：下方「有 b0 约 12/164 tick」**计数偏低**（实测 18/198），且「价 = LE16/1000」对 9:15:09/48 错误。完整复核见第九章。

12 个锚点里，量字段起点在 ts 后的偏移：+4, +5, +6, +7, +8 都出现过。规律：
- **有 b0（价变化）**：`[价 LE16][插入字节][b0] [量 LE24] [未匹配]`，量在 b0 后。实测 18/198 tick
- **无 b0（价不变）**：`[标记字节] [量 LE24] [未匹配]`，标记长度 1 或 2 字节。实测 180/198 tick

试过的规则都只对部分 tick 成立：
- 「+0 字节 bit7=1 → 标记 1 字节，bit7=0 → 2 字节」：只对 9:15:36~48 那 4 条有效
- 「量后必有合理未匹配」：启发式，50% 假量（不能用，客户端是确定性解析）
- 「价 = LE16(ts 后前 2 字节)/1000」：对 9:15:09/48 明显错误（见九章 9.3）

~~**下一步必须破译 fmt 子标记 22/34 的语义**~~ → **23:30 复核：0x22/0x34 非宽度标记（见九章 9.4），价区破译需转 x32dbg 逆向**。

---

## 五、关键文件

### 抓包样本（★ 最新冷启动抓包最重要）

- **`captures_live/superorder_20260726_213855.pcap`** — **★ 冷启动全流程抓包**（2MB，8 条 8901 流，含 init 握手 + 3 只沪市票分时）
  - 这是字段表定位的依据，新会话优先看这个
- **`captures_live/superorder_20260726_213855_resp_stream0.bin`** — **★ 603118 响应流**（599KB，116 帧）
  - **竞价响应帧：off=488747（2654 字节）**，字段表在帧+52（`0a 70 04` 起），9:15:00 时间戳在帧+81
  - 含完整 9:15:00~9:25:00 的 200 条 tick
- `captures_live/superorder_20260726_201429.pcap` — 超级盘口抓包（6.4MB，含 4260/7169 协议，盘中逐笔）
- `captures_live/superorder_20260726_201429_resp_stream1.bin` — 超级盘口响应流（4260，29 字段）
  - 关键帧：off=208180（440KB）。实测偏移：字段表 dt10 起点 rel=115、个股壳 rel=289、9:15:00 ts rel=302、9:15:03 锚点 rel=376
- `captures_live/_sh_auction_resp_603118.bin` — 4214/7176 沪市单帧（2661B，旧抓包，167 条——现知是扫描漏了变体）

### 脚本

- `tests/capture_superorder.py` — **抓包脚本**（抓 + 分析，pcapng 格式，自动拆流）
  - 用法：`py tests/capture_superorder.py --duration 120 --code 603118`
  - 分析：`py tests/capture_superorder.py --analyze-only xxx.pcap --code 603118`
- `tests/test_auction_sh_compare.py` — 多票竞价对比脚本
- `tests/test_auction.py` — 单票竞价测试

### 代码

- `src/thspypc/protocol.py`:
  - `_parse_auction_sh()` — 沪市 4214/7176 解析（**旧逻辑 167 条，待用新时间戳规则重写为 200 条**）
  - `parse_auction_response()` — 调度器（深市 flag=0x003a / 沪市变长）
  - `_auction_ts_in_range()` — 9:15-9:25 时段判断
  - **待重写**：用 `ts = 0x6a620000 + b1*256 + b0`（b1∈{bc,bd,be}）扫时间戳，覆盖变体
  - **待新增**：字段表解析（从 hd1.0 后提取 `[dt][fmt...][0x04]`）、量/未匹配解析（LE24/变长LE）

### 文档

- `HANDOFF_PUSH_INVESTIGATION_20260724.md` — 之前的完整协议调研（分时/竞价/沪深分服）
- `docs/x32dbg_变体A调试手册.md` — hexin.exe 调试方法（方案 A 用）

---

## 六、下一步建议（优先级排序）

> ⚠️ 2026-07-26 23:30 修订：原建议 1（破 fmt 22/34）已在第九章深度复核，**假设未证实**（0x22/0x34 更可能是尾数高位字节而非宽度标记）。原建议 3（有 b0 tick 100% 准确）**也已被证伪**——9:15:09/48 的 LE16 解码错误。请以第九章 9.5 的方向为准。

1. **★ 价区破译需转 x32dbg 逆向**（纯静态已穷尽，见第九章 9.5）
   - 关键：客户端读 dt10 价的变长循环终止条件
   - varint(LEB128) 已排除，疑为位级变体

2. **用新时间戳规则重写 `_parse_auction_sh`**（已可做）
   - 旧逻辑 167 条 → 新规则 199/201 条（用第九章 9.1 的「`[b0,b1]` + 1-2 字节内有 0x62」）
   - 注意：2 字节扫描会有 4 个假阳性，必须用「后续有 0x62」过滤

3. **先落地深市（已完全走通），沪市暂取价**（务实路线）
   - 深市 `parse_auction_response` 的 flag=0x003a 分支已 100% 工作（定长 4B/字段）
   - 沪市量/未匹配可确定性解析（LE24/变长LE），价区用现有 167 条结果兜底

4. **集成到 `client.py`**
   - 复用现有 4214/7176 请求（已实现），改解析端
   - 请求参数：`DT=7176(915-925的unix区间) DataType=10,27,33,49`

5. **回归验证**
   - 603118 9:15-9:25 应有 199 条（缺 9:16:33/36/48 边界）
   - 量/未匹配与 11 锚点对照（价区暂不验证，待逆向）

---

## 七、技术备忘

### 时间戳编码（最终版，2026-07-26 23:30 复核修正）

- **解码公式**：`ts = 0x6a620000 + b1*256 + b0`
- **b1 取值**：0xbc（9:13-9:16 段，竞价用 9:15-9:16）、0xbd（9:16:48-9:20:31）、0xbe（9:20:32-9:25:00）
- **b0**：每 3 秒 +3，跨分钟时 b1 进位、b0 回绕
- **★ 识别规则（23:30 修正）**：`[b0, b1∈{bc,bd,be}]` 且 **frame[off+2]==0x62 或 frame[off+3]==0x62**
  - `0x62` 恒在时间戳里，只是位置在 byte2（标准 `b0 b1 62 6a`）或 byte3（变体 `b0 b1 XX 62`）
  - 变体的 byte2（0xf2/0xe1/0x70 等）是 fmt 标记/字段数据被嵌进时间戳字节流
  - **不要用纯 2 字节 `[b0,b1]` 扫描**——有 4 个假阳性；也不要用严格 4 字节 `[b0,b1,62,6a]`——漏 117 条
- 9:15:00 = 1784855700 = `94 bc 62 6a`，9:25:00 = 1784856300 = `ec be 62 XX`
- body 内 tick **乱序散布**（不同时段交织），去重必须按 ts 排序
- **缺失 3 条**：9:16:33/36/48（b1 进位边界 bc↔bd，编码特殊）→ 实测 **199/201**

### 字段编码（23:30 复核修正）

- **价（dt10）**：`b0` 是 ths_float 的 exp_byte（除 1000），价尾数是 b0 前的变长 LE。**⚠️ 「价 = LE16(b0 前 2 字节)/1000」仅对部分 b0 tick 成立**（9:15:00/03/06/33 ✓，但 9:15:09/48 解出 3.17/6.65 明显错误）。完整规律未破，需逆向（见九章 9.5）
- **量（dt49）**：**LE24 固定 3 字节，单位股**（11 锚点 100% 匹配，字节序列帧内唯一）。如 73600 = `80 1f 01`，3440828 = `bc 80 34`
- **未匹配（dt27）**：变长 LE（去高位 0），单位股，紧大量后。如 700 = `bc 02`（2字节），225100 = `4c 6f 03`（3字节）
- **显示换算**：客户端显示的「竞价量/未匹配量」单位是**手**，协议单位是**股**，÷100（零股四舍五入到小数）
- **金额（dt33）**：编码未知，尾部 `...00 80` 模式疑似记录分隔但非终止符

### 字段表 fmt 标记（23:30 复核：0x22/0x34 非宽度标记）

> 假设「0x22/0x34 编码字段宽度」**未证实**。深市对比：深市字段表 dt10/dt49/dt27/dt33 **全是 fmt=0x70 width=4**（`_parse_hd_field_table` 定长 4B/条）。沪市是变长版本，fmt=0x70 同为 ths_float 类型标记。0x22/0x34 更可能是**尾数高位字节被误读**（如 9:15:21 的 `5e 3d 34 b0`，34 紧邻 b0，是 ths_float 尾数的一部分）。

| 字段 | fmt | 说明 |
|------|-----|------|
| dt10 (价) | `[0x70]` | ths_float 类型标记，变长存储 |
| dt49 (量) | `[0x22, 0x70]` | 0x22 疑为尾数高位字节，非宽度规则（量实际固定 LE24 3 字节） |
| dt27 (未匹配) | `[0x70]` | ths_float 类型标记，变长存储 |
| dt33 (额) | `[0x34, 0x70]` | 0x34 疑为尾数高位字节，非宽度规则 |
| dt33 | `[0x34, 0x70]` | **34 是子标记，语义未破译** |

### 价格编码（ths_float）

- 标准格式：bit31=除法标记，bit30-28=exp，bit27=符号，bit26-0=尾数
- 15.01 = `0xb0003aa2`（exp=3 除 1000，尾数 15010）
- 高价股（如 600276 ~54 元）同样用 exp=3：54.71 = `0xb000d5b6`

### 沪深分服（已实现，复用）

- 沪市：shlv2.123ths.com:8901（5 IP），init MarketCode=16;144;
- 深市：szlv2.123ths.com:8901（9 IP），init MarketCode=32;
- __manual 登录 + init + 4214 订阅是推送通道的必要前置（详见 HANDOFF_PUSH_INVESTIGATION）

### 已验证的锚点（12 个，603118 沪市）

| 时刻 | 价 | 量(股) | 未匹配(股) | 金额(万元) | 来源 |
|------|-----|--------|-----------|-----------|------|
| 9:15:03 | 15.01 | 73600 | 700 | 110.47 | 用户提供 |
| 9:15:15 | 15.47 | 425400 | 11800 | 658.09 | 用户提供 |
| 9:15:36 | 15.43 | 562100 | 5500 | 867.32 | 用户提供 |
| 9:15:39 | 15.45 | 555800 | 1800 | 858.71 | 用户提供 |
| 9:15:42 | 15.43 | 560100 | 3300 | 864.23 | 用户提供 |
| 9:15:45 | 15.43 | 562000 | 1100 | 867.17 | 用户提供 |
| 9:16:54 | 15.35 | 704400 | 61300 | 1081.25 | 用户提供 |
| 9:17:00 | 15.47 | 656100 | 3000 | 1014.99 | 用户提供 |
| 9:17:03 | 15.45 | 660600 | 16200 | 1020.63 | 用户提供 |
| 9:17:09 | 15.43 | 670800 | 5600 | 1035.04 | 用户提供 |
| 9:20:09 | — | 786100 | 225100 | 1206.66 | 用户提供 |
| 9:24:57 | 15.40 | 3440828 | 59172 | 5298.88 | 用户提供 |
| 000938 9:21:03 | 41.15 | 954000 | 1200 | — | 用户提供（深市）|

---

## 八、会话走过的弯路（避免重蹈）

| 尝试 | 结果 |
|------|------|
| ~~4214/7176 服务器只给到 9:22:39~~ | **✗ 错误结论！实际给到 9:25:00 完整 200 条**，是旧扫描漏了变体时间戳 |
| ~~应转向超级盘口 4260/7169~~ | **✗ 错误方向！4214/7176 就是正解**，4260 是盘中逐笔更复杂 |
| 价格用「前价偏差<20%」猜 | △ 治标，应改用 b0 标记确定性定位 |
| 时间戳 4 字节扫描 `[b0,b1,62,6a]` | ✗ 漏 16% 变体 tick（b2≠0x62），应用 `[b0,b1]` + 步长 3 验证 |
| 时间戳按 off 递增去重 | ✗ body 内 tick 乱序，丢整段 |
| 用「合理范围/单调性」猜量字段 | **✗ 用户明确指出不能用启发式**，客户端是确定性解析 |
| 「+0 字节 bit7 决定标记长度」 | ✗ 只对 9:15:36~48 有效，不普适 |
| 「量后必有合理未匹配」启发式 | ✗ 50% 假量，不可用 |
| **冷启动抓包定位字段表** | **✓ 突破！字段表在响应 hd1.0 后，与 DataType 对应** |
| **12 锚点验证量/未匹配编码** | **✓ 量=LE24，未匹配=变长LE，单位股** |
| ~~2 字节扫描得 200/201 条~~ | **✗ 23:30 复核：2 字节扫描有 4 个假阳性。正确规则见第九章，得 199/201** |
| 「价 = LE16(ts后前2字节)/1000」 | **△ 仅对部分 b0 tick 成立**（9:15:09/48 解出 3.17/6.65 明显错误），不是普适规则 |

---

## 九、★ 2026-07-26 23:30 深度复核（路线1：fmt 标记 → 字段宽度验证）

> 目标：验证「fmt 子标记 0x22/0x34 编码字段宽度」假设，攻破字段起始位置漂移。
> 方法：用 11 个锚点的**量字节序列**（LE24，唯一出现）作确定性锚，反向切分每条 tick。

### 9.1 时间戳识别规则（最终修正版）

之前 22:00 写的"2 字节扫描 `[b0,b1]` 得 200/201 条"**有 4 个假阳性**（碰巧满足 ts 落入区间的非时间戳字节）。正确规则：

```
识别: [b0, b1∈{0xbc,0xbd,0xbe}] 且 frame[off+2]==0x62 或 frame[off+3]==0x62
解码: ts = 0x6a620000 + b1*256 + b0
```

**关键洞察**：`0x62` 恒在时间戳里，只是位置在 byte2（标准 `b0 b1 62 6a`）或 byte3（变体 `b0 b1 XX 62`）。变体的 byte2（0xf2/0xe1/0x70 等）是**字段表 fmt 标记或字段数据被嵌进了时间戳字节流**。实测：

| 扫描规则 | 命中/201 | 问题 |
|---|---|---|
| 严格 4 字节 `[b0,b1,62,6a]` | 84 | 漏 byte3≠0x6a 的 tick |
| 宽松 3 字节 `[b0,b1,62]` | 165 | 漏 byte2≠0x62 的 tick |
| 宽松 2 字节 `[b0,b1]` | 205 | **4 个假阳性** |
| **★ 新规则：`[b0,b1]` + 1-2 字节内有 0x62** | **199** | 仅缺 9:16:33/36/48（b1 进位边界） |

### 9.2 字段切分结构（11 锚点确定性验证）

每条 tick = `[ts 4B] [价区 变长] [量 LE24 3B] [未匹配 变长 2-3B] [额区 变长]`：

- **量（dt49）= LE24 定长 3 字节**：11 锚点 100% 匹配，且**字节序列在帧内唯一出现**（可作确定性锚定位）
- **未匹配（dt27）= 变长 LE**：去高位 0，2 字节（<65536）或 3 字节，紧大量后（无间隔）
- **额（dt33）= 编码未知**：未匹配后到下条 ts，未见干净 LE/ths_float 解码

### 9.3 价区（核心未解难点）— 部分规律 + 明确失败边界

**已确认的部分规律**：
- `b0` 是 **ths_float 的 exp_byte**（值的最后一字节），不是字段分隔符。`0xb0` = divide(1) + exp(3) → 除 1000
- 深市（SZ）对比证实：深市 dt10/dt49/dt27/dt33 **全是 fmt=0x70 width=4 的 ths_float**（深市字段表 `_parse_hd_field_table` 返回 `(dt,fmt,width)`，fmt=0x70 → `decode_ths_float`）
- 沪市是深市的**变长版本**：fmt=0x70 同样是 ths_float，但变长存储（省略 LE32 中的 0x00 字节）
- 含 b0 的 tick（18/198）：价 = LE16(b0 前 2 字节)/1000 在**多数锚点成立**（9:15:00=16.43✓、9:15:03=15.01✓、9:15:06=15.43✓、9:15:33=15.45✓）

**明确失败的边界**（不能直接用「LE16(前2字节)/1000」）：
- 9:15:09 `[60 0c 3b b0]` → LE16(60 0c)=3168 → **3.168 ✗**（应 ~15.4x）
- 9:15:48 `[f6 19 3b b0]` → LE16(f6 19)=6646 → **6.646 ✗**
- 这两条的「中间字节」(3b) 前的字节不是价尾数 —— 说明**价尾数有时不是从 ts+4 开始**，前面可能插了别的字段字节

**价区长度分布**（198 个 tick，到第一个 b0）：`{0:2, 1:1, 2:4, 3:9, 4:1, 8:1}`，**无固定长度规则**。

**无 b0 的 tick（180/198）**：价不出现，沿用前值。价区只有 1-2 字节，含义未破译（疑为 dt33 额的增量或标志位）。

### 9.4 fmt 子标记 0x22/0x34 验证结论

**假设「0x22/0x34 编码字段宽度」未被证实**：
- dt49(量) fmt=`[0x22,0x70]`，但实际量字段宽度固定 3 字节（LE24），与 0x22 无明显对应
- dt33 fmt=`[0x34,0x70]`，0x34 偶尔出现在 tick 数据里（如 9:15:21 的 `5e 3d 34 b0`），但**紧邻 b0**，更像是 ths_float 的尾数高位字节而非宽度标记
- **0x70 是类型标记（ths_float），0x22/0x34 很可能是尾数高位字节被误读为「子标记」**

字段表的实际格式（变长版）：每条 = `[dt][fmt字节...][0x04分隔]`，其中 fmt 字节就是该字段 ths_float 值的 exp_byte 之类——**字段表本身记录的是「类型的 exp 默认值」，不是宽度规则**。

### 9.5 下一步明确方向（给后续会话）

1. **价区破译需要 x32dbg 逆向**（纯静态已穷尽）：
   - 目标函数：hexin.exe 里解析 dt10(价) 的变长读取循环
   - 关键问题：客户端如何决定「ts 后读几个字节作为价尾数」—— 18/198 含 b0 的 tick 里，价区长度从 0 到 8 字节都有，必然有一个**读循环的终止条件**（如「读到 exp_byte 为止」+「尾数按 varint」），但 varint(LEB128) 已验证不符
   - 备选：可能用了**位级 varint**（bit7 继续位，但组合方式非标准 LEB128）

2. **可立即落地的部分解析**（不需要破价区）：
   - **量/未匹配可确定性解析**：先用 2 字节 ts 扫描定位，然后对每个 ts 候选，在 ts+4~ts+30 窗口内搜【量字节序列】（需先用 ts 前几个 tick 建立量基准，因为量字节本身不唯一）
   - 或：**先解深市**（已完全走通，定长 4B/字段），沪市暂只取价（沿用现有 `_parse_auction_sh` 的 167 条结果）

3. **不要再用启发式猜价**：9.3 已证明「LE16(前2字节)/1000」对 9:15:09/48 明显错误，继续用会污染数据

---

## 十、★★★ 2026-07-27 跨样本对比突破：参数化变长编码（颠覆性发现）

> 用户提出「多抓数据做对比，把协议整体搞清楚」。对现有 4 个沪市样本（3 只票 × 多个抓包）做跨样本对比后，**推翻了第九章「价区规律未破」的判断**——真相是沪市用了**参数化变长编码**，之前在单个抓包里找统一规律注定失败。

### 10.1 决定性证据：同一只票不同抓包，字段表完全不同

同一只 **603118**，三个不同抓包的 dt49/dt33 fmt 子标记：

| 样本 | dt49 fmt | dt33 fmt | 备注 |
|------|---------|---------|------|
| 603118 单帧 | `[0x11,0x70]` | `[0x1a,0x70]` | N=1 基准 |
| 603118 superorder(213855) | `[0x22,0x70]` | `[0x34,0x70]` | **N=2（恰好 2 倍）** |
| 601318 单帧（60 元）| `[0x70,0x44]` | `[0x70,0x68]` | **N=4，且 fmt 字节顺序相反** |
| 600276 单帧（54 元）| `[0x11,0x70]` | `[0x1a,0x70]` | N=1（同 603118 单帧）|

**关键结论**：
- fmt 子标记 **不是固定语义**，是**该响应的编码参数**，随每次请求动态变化
- 603118 vs 600276（价位 15 vs 54，差 3.6 倍）fmt **完全相同** → 参数**不是由价位决定**
- 同一只 603118 单帧 vs superorder，fmt 从 `[11,1a]` 变 `[22,34]` → 服务器对同一只票可用不同参数编码
- N 值呈 1/2/4 倍数关系，dt49 基准 17、dt33 基准 26（比例恒定 17:26）

### 10.2 插入字节 = fmt 子标记的派生（×3 恒定关系）

同一只 603118、**同一时刻 9:15:03**，两个抓包的价字段：

```
单帧(fmt dt49=0x11):   a2 3a [0x33] b0   ← 插入字节 0x33 = 51
super(fmt dt49=0x22):  a2 3a [0x66] b0   ← 插入字节 0x66 = 102
```

`51 = 17 × 3`，`102 = 34 × 3` → **插入字节 = fmt 子标记 × 3**（恒定比例）。

之前第九章困惑的「价区中间字节（66/33/34）是什么」，答案：**它是 fmt 参数在字段数据里的派生**，控制该字段的变长编码方式。

### 10.3 ts 也有插入字节（解释了第九章的「ts 变体」之谜）

第九章说「ts byte2 有变体（0xf2/0xe1/0x70）」，跨样本对比后真相浮现：

```
603118 单帧: ts = 94 bc 62 6a           (4 字节)
600276:      ts = 96 bc [0x64] 62 6a    (5 字节，插入 0x64=100)
601318:      ts = 97 bc [0x64] 62 6a    (9:15:03)
             ts = a0 bc [0x89] 62 6a    (9:15:12，插入字节变了！)
```

**ts 的「变体字节」和字段表 fmt 参数是同一套编码体系**——之前当成 ts 扫描噪声是错的，它是参数化编码的一部分。

### 10.4 同一时刻不同抓包，价字段结构不同

```
603118 9:15:00:  单帧 2e 40 [4c] b0  | super 2e 40 b0       ← 单帧有插入，super 无
603118 9:15:09:  单帧 60 3b b0       | super 60 [0c] 3b b0  ← 单帧无插入，super 有
```

**插入字节的存在与否、位置，完全由该响应的 fmt 参数（编码模式）决定**，不是 tick 内容决定的。

### 10.5 核心结论：沪市竞价 = 参数化变长编码

旧理解（错）：字段有固定编码规则，在单个抓包里能找到。
**新理解（对）**：服务器每次响应选一个**编码参数 N**（observed N∈{1,2,4}），所有字段（ts/价/量/未匹配/额）的变长方式都按 N 缩放。fmt 子标记 = 基准值 × N。

| 字段 | 基准(N=1) | N=2 | N=4 |
|------|----------|-----|-----|
| dt49 fmt | 0x11(17) | 0x22(34) | 0x44(68) |
| dt33 fmt | 0x1a(26) | 0x34(52) | 0x68(104) |
| 价插入字节 | 基准×3 | 基准×3 | 基准×3 |

**这解释了第九章所有「规律只对部分 tick 成立」的困境**——不是规律错，是不同抓包用了不同 N，混在一起分析当然找不到统一规则。

### 10.6 下一步（修订）

1. **★ 抓更多样本，锁定 N 的取值与触发条件**（见下方「需要补充的数据」）
2. **对每个 N 单独建立解码规则**：N=1/2/4 各自一套字段宽度表，抓包时先读 fmt 参数确定 N，再套对应规则
3. **仍需 x32dbg 逆向**：确认 N 如何决定每个字段的具体宽度（N 与字段字节数的映射函数）
4. **跨样本验证「插入字节 = fmt×3」**：当前只有 603118 两个抓包验证，需更多样本确认这个 ×3 是否普适

### 10.7 需要补充的数据（给用户）

为彻底锁定参数化编码，需要：

1. **同一只票（如 603118）多次抓竞价响应**（5-10 次），看 fmt 参数是否每次都变、N 的取值范围
   - 目的：确认 N∈{1,2,4} 还是连续值，以及 N 是否随机
2. **多只不同价位的沪市票**（低价 5 元、中价 30 元、高价 100 元、高价 300 元如茅台），各抓 1-2 次
   - 目的：确认 N 与价位/市值是否相关（目前看无关，但样本太少）
3. **同一只票同一会话内，连续多次请求**（hexin 客户端反复打开竞价面板）
   - 目的：看 N 是否在一次会话内保持稳定，还是会话内也变
4. **如果能抓到带 dt33(额) 非零的时段**（9:20 后未匹配量大的时刻）
   - 目的：额字段编码是最后未解的字段

★ 抓包用现有 `tests/capture_superorder.py` 或单帧请求脚本即可，关键是要**记录每次抓包的 fmt 参数**（dt49/dt33 子标记），这是对比的基础。

### 10.8 ★★★ 2026-07-27 实抓验证：fmt 子标记 = 字段宽度开关（决定性证据）

> 用户跑了 `tests/capture_auction_raw.py 603118 --repeat 5`（同票同会话 5 次抓包）。
> **5 次响应分成两组，字段表完全不同**，直接验证了「参数化变长编码」并锁定子标记语义。

#### 5 次抓包的字段表（同一只 603118，同一会话）

| 次数 | dt10(价) | dt49(量) | dt27(未匹配) | dt33(额) | 组 |
|------|---------|---------|------------|---------|-----|
| r1 | `[0x70]` | `[0x11,0x70]` | `[0x70]` | `[0x1a,0x70]` | **A** |
| r2 | `[0x11,0x70]` | `[0x70]` | `[0x11,0x70]` | `[0x70]` | **B** |
| r3 | `[0x11,0x70]` | `[0x70]` | `[0x11,0x70]` | `[0x70]` | **B** |
| r4 | `[0x70]` | `[0x11,0x70]` | `[0x70]` | `[0x1a,0x70]` | **A** |
| r5 | `[0x70]` | `[0x11,0x70]` | `[0x70]` | `[0x1a,0x70]` | **A** |

**关键观察**：
- `0x11` 子标记**不是固定附在某个字段上**——组 A 附在 dt49（量），组 B 附在 dt10（价）+ dt27（未匹配）
- 同一会话内服务器**随机**返回两种编码（A 3 次 / B 2 次），无规律
- 这推翻了第十章 10.1「N=1/2/4 倍数关系」的猜测——真相更简单：**`0x11` 是「该字段用变长(去高位0)编码」的开关标记**

#### 同一时刻 tick 的字节对比（9:15:01，两组都有）

```
组A (dt49=[0x11]):  ts=15 b1 66 6a | 6e 37 b0 | 58 33 02 | 00 00 26 80
组B (dt49=[0x70]):  ts=15 b1 66 6a | 6e 37 93 b0 | 58 02 | 32 00 00 80
```

- ts 相同（`15 b1 66 6a`）
- 价尾数相同（`6e 37`），但组 B 多了插入字节 `93`（价字段带了 0x11 开关 → 变长）
- **量字段宽度不同**：组 A `58 33 02`（3 字节 LE24，因 dt49 带 0x11），组 B `58 02`（2 字节，因 dt49 不带 0x11，去高位 0）

**结论**：**fmt 子标记 `0x11` = 「该字段值去高位 0x00 字节（变长存储）」的开关**。带 0x11 的字段按实际值宽度存（省略前导 0），不带 0x11 的字段按固定宽度存。客户端读字段表时，先看该字段有没有 0x11，决定读取方式。

#### 这解释了之前所有困惑

- 第九章「价区规律混乱」：因为不同抓包用不同编码（A/B 混在一起分析）
- 第十章「N=1/2/4」猜测：错误，实际是「0x11 开关附在不同字段上」
- superorder 抓包 dt49=`[0x22,0x70]`：0x22 可能是另一种宽度标记（待验证是否也是开关）

#### 顺带修复的 bug（2026-07-27）

抓包过程中发现 `_parse_auction_sh` 的 ts 扫描写死了 7-24 的字节范围（b1∈0xbc..0xbe, byte2=0x62），对 7-27 的数据（b1∈0xb1..0xb3, byte2=0x66）完全扫不到 → 解析返回空 → `_auction_query_once` 循环读不到竞价帧 → **超时**。已修复：改为扫 `byte3=0x6a` + `_auction_ts_in_range`（跨日通用）过滤。修复后 7-27 数据解出 79~85 条。

另：`build_auction_query` 在 `trade_date=None` 时发 `DateTime=7176(0-0)`，服务器不响应竞价数据（会超时）。抓包脚本 `capture_auction_raw.py` 已改为默认算最近交易日。

#### 下一步（修订）

1. **★ 验证「0x11 = 变长开关」假设**：对组 A/B 的同一时刻 tick，按「带 0x11 的字段去高位 0、不带的固定宽度」规则切分，看是否两组都能正确解出价/量/未匹配
2. **破译其他子标记**（0x1a/0x22/0x34/0x44/0x68）：用同样的「同时刻 tick 对比」方法，看它们各自控制什么
3. **仍需 x32dbg 确认**：客户端读字段表后如何根据子标记决定读取循环（纯静态已基本锁定，逆向可验证）

### 10.9 ★ bit7(0x80) 差异确认 + varint 假设（2026-07-27 续）

对组 A/B 同一时刻含 b0 的 tick 做字节级对比，发现决定性规律：

```
9:24:13: A=...44 b0...   B=...e4 b0...   ← 0x44 vs 0xe4 差 0x80
9:24:37: A=...e4 b0...   B=...44 b0...   ← 互换！
```

**同一字段值，组 A/B 的某字节差 0x80（bit7 翻转），且在相邻 tick 里互换**。这强烈暗示 **bit7 是 varint 的「继续位」**：
- `0x44`（bit7=0）= 该字段值在此字节结束
- `0xe4`（bit7=1）= 字段值延续到下一字节

字段表的 `0x11` 子标记很可能控制「该字段是否用 varint 编码」：带 0x11 的字段用 bit7-varint，不带则固定宽度。

**但 varint 的确切分组方式无法纯静态确定**（尝试 LEB128 标准解读，价尾数 `6e 37` 用 varint 解出 110 而非 14190；混合 LE16+varint 也不自洽）。同一字段值在不同编码下的字节序列差异复杂（字节数变化 + bit7 翻转 + 字段表 0x11 移动三者交织），**纯静态对比已到极限**。

### 10.10 最终结论与下一步

**已彻底确认的**（纯静态）：
- ts 扫描规则（byte3=0x6a + 时段过滤，跨日通用）✓
- 字段表格式（`[dt][fmt...][0x04]`，dt10/49/27/33 四字段）✓
- fmt `0x70` = ths_float 类型标记 ✓
- **bit7(0x80) 差异是真实的**（组 A/B 同时刻字节 44↔e4 互换）✓
- **0x11 子标记位置可移动**（同会话不同响应附在不同字段）✓

**必须 x32dbg 逆向才能确定的**：
- varint 的确切分组规则（每 7bit？混合字节？）
- 0x11/0x1a 等子标记如何决定读取循环
- 「价字段何时出现」（b0 标记的判定条件）

**逆向目标**：hexin.exe 里读取沪市竞价响应、按字段表解析 tick 的函数。可用 `docs/x32dbg_变体A调试手册.md` 的断点方法，在拿到解析后的字段值后反推读取循环逻辑。

**当前可落地的**：组 A 格式（dt10=`[70]` 固定、dt49=`[0x11]` 变长）用「找 b0 + LE16 价 + LE24 量」能解出价（7-27 验证价 14.19 ✓），可作为**部分可用解析器**先用着，量/未匹配的完整解析待逆向。

---

## 十一、★★★ 2026-07-27 晚：thsdk 协议独立性 + 跨协议逐点对照方案

> 本节推翻第十会话的一个潜在误判：**thsdk 解出来的竞价字段，不等于 thspypc
> 抓的 hexin.exe 协议字段**——两者是同花顺的两套独立行情通道。但恰恰因为这个
> 差异，thsdk 可以作为**跨协议 ground truth**，用逐点对照攻克 thspypc 自己的
> 变长编码。本节给出可执行的对照方案。

### 11.1 决定性发现：thsdk 与 hexin.exe 是两套不同协议

`D:\code\gh\thsdk` 是一个独立 SDK（`pip install thsdk`，v1.7.18）。Python 层
只是壳，所有解析在编译好的动态库 `hq.dll`/`hq.dylib` 里（每平台 ~8MB）。用
二进制字符串扫描 `hq.dll`（windows 版）得到铁证：

**thsdk 是 Go 写的，包路径 `thshq/internal/core`**，关键符号：

```
thshq/internal/core.UnifiedLogin        ← RSA 登录
thshq/internal/core.AuthPublicKeyV1     ← RSA 公钥认证
thshq/internal/core.BuildInitData
thshq/internal/core.ParseDataHqFile     ← Hq 文件解析
thshq/internal/core.ParsePacketHead
thshq/internal/core.encodeHXSigature    ← HX 签名
thshq.CodeListItem / ParseCodeList     ← 代码列表
thshq.Quote / NewQuote / ParseInfo     ← 行情解析
源文件: codelist.go / login.go / packet.go / parse.go
```

**服务器/端口/登录全不同**：

| 维度 | thsdk（Mac/Win/Linux 同源 Go）| thspypc 抓的 hexin.exe |
|------|------------------------------|----------------------|
| 服务器域名 | `4534maczhuhq1` / `macfu1` / `macfunction1` `.123ths.com` | `shlv2` / `szlv2` `.123ths.com` |
| 端口 | **9602-9605** | **8901** |
| `lv2` 字样 | **0 次**（`shlv2`/`szlv2`/`lv2` 全 0）| 全程使用 |
| 登录身份 | RSA `UnifiedLogin`，无 `__manual` | 必须 `__manual` 注册推送 |
| `pageid 4214` / 周期 `7176` | **0 次出现** | thspypc 竞价全靠这两个 |
| 实现语言 | Go（thshq 包）| C/C++（hexin.exe）|

**关键结论**：
1. **Mac thsdk ↔ Windows thsdk**：✅ **同一份协议**。同一 Go 源码树交叉编译，
   域名都带 `mac` 前缀（原为 Mac 客户端做的 SDK）。Windows dll 跑出的字段 =
   Mac 版会看到的字段。
2. **thsdk 协议 ↔ thspypc 的 hexin.exe 协议**：❌ **明确不是同一份**。两套并行
   的行情接入通道，服务器、端口、登录、协议标识全不同。

**⚠ 对后续会话的警告**：
- thsdk 返回的「买2量/卖2量」**不能直接等同于** hexin 协议（4214/7176）的字段。
  thsdk 解的是 Go 协议（`ParseInfo`/`GetDataType`），不是 thspypc 的 dt27。
- **反编译 thsdk 的 dylib 没用**——拿到的是 Mac/Go 协议逻辑，不是 thspypc 的
  4214/7176 逻辑，混进来会污染 thspypc。第十会话"路线 B 反编译 dylib"作废。
- thsdk 能解出沪市竞价，**不代表** thspypc 的 4214/7176 协议层也被解了。
  thspypc 卡住的 varint 仍只存在于自己抓的那份协议里。

### 11.2 thsdk 竞价字段实测（2026-07-27 周一盘后）

`tests/probe_thsdk_auction.py` 实跑（无需 L2，游客账户即可）。沪深两市字段：

```
字段名全集（5 个）: ['时间', '价格', '买2量', '卖2量', '当前量']
```

⚠ README 示例字段名（成交方向/成交量/委托买入价/委托卖出价）是**占位错误**，
`call_auction.py` 示例漏了「价格」。实测以库返回为准。

**沪市 603118（200 条，间隔全 3 秒）**：
```
首条 09:15:01  价14.19  买2量=0x80000000(无)  卖2量=11500   当前量=600
末条 09:24:58  价14.60  买2量=98203           卖2量=0x80000000(无) 当前量=3278897
```

**沪深关键差异**（8 票采集，`tests/probe_thsdk_auction_sign.py`）：

| 维度 | 沪市（5 票一致）| 深市（3 票一致）|
|------|----------------|----------------|
| 无值标记 | `0x80000000` 哨兵 | `0` |
| 买2/卖2能同时非0 | **不能**（双实=0）| **能**（双实=全部条数）|
| 撮合频率 | 每 3 秒（149-200 条）| 每 9 秒（59-68 条）|

**关键矛盾**：沪市买2/卖2互斥（似单字段拆分），深市并存（似双独立字段）。
**仅凭 thsdk 输出无法判定 hexin 协议层是单字段还是双字段**——这是 thsdk 内部
处理后的结果，不是协议原始字节。要定论必须回 thspypc 抓包对照（见 11.4）。

`0x80000000` 哨兵 = 2147483648，表示该方向无未匹配委托。集合竞价撮合时被动方
总被吃光，故每条 tick 恰好一侧为哨兵（极少数双哨兵=双方都无未匹配）。

### 11.3 ★ 跨协议逐点对照方案的可行性

thsdk 与 thspypc 协议不同，但请求的是**同一只票、同一时刻、同一交易所撮合结果**。
集合竞价撮合是交易所 centralized 的客观事实——9:15:03 沪市 603118 的撮合价/
累计量/未匹配量，任何通道查到的值都一样（数据源都是上交所）。**这是对照基础**。

**与第九/十章纯静态分析的本质区别**：之前只有字节没答案，800 个变长 tick 里
猜边界。thsdk 提供 **200 条 tick × 5 字段 = 1000 个已知值**，把"猜边界"变成
"已知答案反查字节位置"——数学上可解。

**字段对照可行性评估**：

| 字段 | thsdk 来源 | 对照可行性 | 把握 |
|------|-----------|-----------|------|
| 时间戳 ts | thsdk「时间」| 已解决（thspypc ts 扫描已通）| ✅ 100% |
| 价（dt10）| thsdk「价格」=撮合价 | **高**——客观一致 | ✅ 90% |
| 当前量（dt49）| thsdk「当前量」=累计量 | **最高**——累计单调，字节序列几乎唯一，强力锚点 | ✅ 95% |
| 未匹配（dt27）| thsdk「买2量」「卖2量」| **中**——需先解 thsdk 拆分↔单值映射（沪用符号、深双字段）| ⚠ 60% |
| 额（dt33）| thsdk 无此字段 | **低**——推导公式存疑（当前量非纯累计，见 11.5），降最低优先级 | ⚠ 见 11.5 |

**dt33 额的推导**（关键，补全 thsdk 不返回的额）：
集合竞价每 tick 的额增量 = 当前量增量 × 该 tick 撮合价，累计即 dt33。
`额[t] = Σ_{i≤t} (当前量[i]-当前量[i-1]) × 价格[i]`，`当前量[0]=0`。
量价都从 thsdk 拿，dt33 ground truth 可纯算术推出，无需 thsdk 返回。

### 11.4 对照执行方案（明晚 7-28 续）

**前提（go/no-go 关卡）**：thspypc 必须抓到 **7-27 同一交易日**的 603118 原始
字节。thsdk 已抓 7-27；thspypc 用：
```bash
py tests/capture_auction_raw.py 603118 --date 2026-07-27
# 产物: captures_live/auction_raw_603118_<ts>.bin
```
⚠ `capture_auction_raw.py` 需 `.env` 的 **level2 账号**（thspypc 走 lv2 域名），
shlv2 IP 偶有超时/CodeListSize=0。**抓到则方案成立，抓不到需换思路**（如重新
逆向 hexin.exe，x86 比 Go dylib 难）。

**对照流程**（抓到 .bin 后）：

```
对每个 ts（共 200 个）：
  1. thsdk 给：{ts, 价, 买2量, 卖2量, 当前量}  ← 已知答案
  2. thspypc .bin 里：用 ts 扫描定位该 tick 字节区段（ts 扫描已通）
  3. 区段内搜「价的 ths_float 编码」→ 价字段占哪几个字节
  4. 搜「当前量的 LE 编码」→ 量字段边界（累计量单调，最稳锚点）
  5. 记录 (字段, 值, 偏移, 字节序列)
归纳：200 × 4 字段 = 800 个 (值→字节) 样本 → 提取编码规则
```

**字段优先级**（按可行性）：
1. **当前量（dt49）**——累计单调，每值字节序列几乎唯一，可直接 grep 定位。
   先解它，作为后续字段边界的基准锚。
2. **价（dt10）**——撮合价客观一致，用 ths_float 编码（exp=3 除 1000）反查。
3. **未匹配（dt27）**——沪市需把 thsdk「买2量−卖2量」合成带符号单值对照
   （买2为正、卖2为负、哨兵侧为 0）；深市可能直接对照双字段。
4. **额（dt33）**——⚠ 推导公式存疑（11.5：当前量非纯累计）。降最低优先级，
   等量价边界定下来后，用 thspypc 字节流里 dt33 实际字节反推公式。

**输出**：归纳出「字段表 fmt 子标记（0x11/0x1a/0x22/0x34）↔ 字段宽度/编码方式」
的映射规则，回写 `_parse_auction_sh`，用 thsdk 数据回归。

### 11.5 dt33 额推导（thsdk 不返回额的补救）——⚠ 公式需修正

**⚠ 2026-07-27 实测发现：「当前量」不是纯累计量**。603118 的 200 条里有
**30 个负增量**（如 9:16:07 当前量从 925400 跳回 777200）。原因是集合竞价
9:15-9:20 是**虚拟撮合**（试探性撮合价/量），每次新委托进来会重新撮合，累计量
上下波动；9:20 后（开放式竞价）才稳定单调递增。

```
9:19:58  当前量=972300   ← 9:20 前还在波动（虚拟撮合重算）
9:20:58  当前量=1090800  ← 9:20 后稳定递增
9:24:58  当前量=3278897  ← 末根（真实开盘累计成交）
```

**因此简单的「逐 tick 量增量 × 价」累加会错**（9:20 前的负增量会减损累计额）。
603118 实测：逐 tick 累加得 4667 万，末量×末价得 4787 万，差 2.5%。

**正确的额推导**（待明晚对照时验证）：

1. **方案 1（最可能）**：dt33 是「**撮合价 × 累计成交量**」的简单乘积，
   即 `额 = 当前量[t] × 价格[t]`。集合竞价的「累计成交量」是当前撮合状态下的
   已成交量，额就是它的货币化。末根：`3278897 × 14.6 = 4787 万`。
2. **方案 2**：dt33 是 9:20 后稳定段的逐 tick 增量累加（剔除 9:20 前虚拟撮合
   的波动）。需先识别"稳定起点"再累加。
3. **方案 3**：dt33 语义完全不同（如委托额而非成交额），需对照字节定论。

**验证锚点**（文档九章 603118 9:15:03）：量 73600 股、价 15.01、额 110.47 万。
`73600 × 15.01 = 110.47 万` ✓ —— 这支持**方案 1**（量×价）。但 9:15:03 是
9:20 前的虚拟撮合点，单点吻合不能排除其他方案，需多点对照。

⚠ **明晚对照优先级调整**：dt33 因推导公式存疑，降为**最低优先级**。先攻
当前量（dt49）和价（dt10），这两个 thsdk 直接给、语义无歧义。dt33 等量价
边界定下来后，用 thspypc 字节流里 dt33 的实际字节反推公式（有字节+量价基准，
反推一个乘积公式很容易）。

⚠ 精度：thsdk 价格是 float（如 14.6），协议层可能是定点（÷1000）。对照时
若额对不上，检查是否价格精度丢失导致（用 thspypc 解出的定点价重算）。

### 11.6 本会话产物

- `tests/probe_thsdk_auction.py` — thsdk 单票竞价探针（字段名/间隔/全量导出）
- `tests/probe_thsdk_auction_sign.py` — 8 票沪深对照采集（哨兵/方向分析）
- `captures_live/_thsdk_auction_USHA*_1785132093.json` — 5 只沪市票全量（ground truth）
- `captures_live/_thsdk_auction_USZ*_1785132093.json` — 3 只深市票全量
- `captures_live/_thsdk_auction_sign_summary_1785132093.json` — 沪深哨兵汇总
- `captures_live/_thsdk_auction_USHA603118_1785131570.json` — 603118 首次采集

**ground truth 已就绪，缺 thspypc 同日 .bin**。明晚（7-28）第一步：抓 603118
的 7-27 原始字节，进入对照。

### 11.7 待解疑点（明晚对照时验证）

1. **hexin 协议 dt27 是单字段还是双字段**：thsdk 沪市互斥（似拆分）、深市并存
   （似双字段）。需 thspypc 字节对照定论。若 hexin 是单带符号字段，thspypc
   解析时应输出带符号 dt27（正=买盘剩余、负=卖盘剩余），不照搬 thsdk 拆分。
2. **thsdk「买2量−卖2量」合成的符号规则**：当前推断"哨兵侧=0、另一侧为正"，
   但沪市双哨兵 tick（如 600519 有 20 条）合成结果=0，是否符合 hexin dt27
   语义待字节对照确认。
3. **fmt 子标记 0x11/0x1a 的真实语义**：第九/十章假设是"varint 开关/宽度标记"
   但未证实。逐点对照 800 样本后应能定论（这是 thspypc varint 最后一块）。

---

## 十二、★★★ 2026-07-27 后续会话：原始响应抓通 + 时间戳变体确认

### 12.1 原始抓包已成功

通过：

```bash
py tests/capture_auction_raw.py 603118 --date 2026-07-27
```

成功建立主行情连接、沪市 `__manual` 连接、init 和 4214 订阅，得到原始响应：

```text
captures_live/auction_raw_603118_20260727_161716_r1.bin
大小：2652B
```

这解决了第 11 章的 go/no-go 关卡：**同日 thsdk ground truth 与 thspypc 原始字节已经同时具备**。

### 12.2 两个客户端 bug 已定位并修复

1. `_auction_query_once()` 原先用 `if b"hd1.0" in resp` 过滤响应。
   沪市真实响应可能是 `hd\x93 1.0` 等变体，不含字面量 `hd1.0`，因此会被直接丢弃。
   现在所有响应交给 `parse_auction_response()`，由协议层区分标准帧和沪市变长帧。

2. 竞价查询原先对每个无关帧重新使用完整 timeout，最多可能阻塞 `20 × timeout`。
   现在 timeout 是整次竞价查询的总等待上限。

涉及文件：

- `src/thspypc/client.py`
- `tests/capture_auction_raw.py`

### 12.3 字段表壳格式扩大

本次响应的字段表起点为偏移 40，内容为：

```text
dt10 = [0x70]
dt49 = [0x70, 0x88]
dt27 = [0x70]
dt33 = [0x70, 0xd0]
```

字段表后个股壳不是旧样本中的：

```text
0x11 + "603118"
```

而是：

```text
0x11 + "6M03118"
```

因此 `tests/capture_auction_raw.py` 的字段表识别已兼容两种壳格式：
`6 位数字`以及`数字 + 市场字节 + 5 位数字`。

### 12.4 时间戳变体的最终实测规则

本次 7 月 27 日响应中，标准时间戳类似：

```text
[b0, b1, 0x66, 0x6a]
```

但也存在：

```text
[b0, b1, 插入字节, 0x66, ...]
[b0, b1, 0x66, 插入字节]
```

因此不能只扫描 `byte3 == 0x6a`。当前解析规则为：

```text
[b0, b1] 后 1-2 字节内出现日期标记 0x62 或 0x66
ts = 0x6a<日期标记><b1><b0>
```

再用 `_auction_ts_in_range()` 过滤时分秒范围。变体扫描会产生少量时间范围内的假阳性，
利用沪市每 3 秒一条的事实，按候选 `ts % 3` 众数过滤。

对本次原始响应：

```text
旧扫描：83 条
变体扫描候选：201 条
按 3 秒节拍过滤后：198 条
```

当时的扫描器得到 198 条，全部能在 7 月 27 日 thsdk ground truth 中找到对应时间点；
尚未识别：

```text
09:18:04
09:24:07
```

第 13.6 节已进一步确认，这两条不是响应缺失，而是控制字节插在时间戳 b0/b1
之间，超出了当时扫描器覆盖范围。

### 12.5 当前字段破解进度

用同日 thsdk 数据逐点对照后确认：

- 当前旧价格启发式在 198 条中有 **171 条价格不匹配**，不能继续作为正式解析规则；
- 当前响应中，dt49 的 3 字节 LE 值在大多数 tick 中可作为原始字节的有序子序列定位；
- 198 条中有 194 条可在时间戳后约 20 字节窗口内，以“3 个目标字节、允许插入 0-1 字节”的方式定位量字段；
- 仍有 4 条量字段无法用该简单规则定位：09:17:16、09:19:01、09:19:46、09:20:07；
- 本次响应的 `dt49=[0x70,0x88]` 与此前 `[0x11,0x70]` 样本不同，进一步证明 fmt 参数会改变字段内插入/变长方式。

### 12.6 下一步

1. 以本次 `.bin` 的 198 个时间点为基准，建立离线逐点对照器，输出每条 tick 的：
   `ts offset / dt49 字节位置 / 插入字节 / dt10 候选区 / fmt`。
2. 先完整归纳 `dt49=[0x70,0x88]` 的编码，不与 `[0x11,0x70]` 样本混合。
3. 解决上述 4 条 dt49 异常记录后，再用 dt49 边界反推 dt10 价格区。
4. 暂不把当前价格启发式回写为正式逻辑；dt27、dt33 继续后置。

---

## 十三、★★★ 2026-07-27 续：dt49 的前值状态压缩已确认

### 13.1 离线逐点对照器

新增：

```text
tests/analyze_auction_varlen.py
```

它读取原始 `.bin` 和同日 thsdk JSON，复现时间戳扫描，并逐 tick 输出：

```text
time / ts_off / volume / omit / payload / positions / inserted / dt10_candidate
```

其中 `omit` 是当前 LE24 数量与前一 tick 相同、因而可省略的低位前缀字节数。
分析器只属于逆向工具，不把 thsdk oracle 依赖带入正式协议层。

### 13.2 四条 dt49「异常」已全部解释

此前简单规则无法定位的 4 条：

```text
09:17:16、09:19:01、09:19:46、09:20:07
```

并不是坏记录，也不是 thsdk 对时误差，而是 **dt49 使用前值状态压缩**：

| 时间 | 前值 → 当前值 | LE24 | 实际行为 |
|---|---:|---|---|
| 09:17:16 | 990000 → 996400 | `30 34 0f` | 低字节 `30` 未变，只发送 `34 0f` |
| 09:19:01 | 896000 → 896000 | `00 ac 0d` | 整个 dt49 不变，字段整体省略 |
| 09:19:46 | 954800 → 954800 | `b0 91 0e` | 整个 dt49 不变，字段整体省略 |
| 09:20:07 | 950400 → 963200 | `80 b2 0e` | 低字节 `80` 未变，只发送 `b2 0e` |

同一日两份不同 fmt 抓包均呈现相同的省略结果，排除了偶然字节命中的可能。

完整 198 tick 的变化模式为：

```text
omit=0（发送完整 LE24 后缀）：194 条
omit=1（省略相同低字节）：    2 条
omit=3（字段完全不变/省略）：  2 条
```

按“省略与前值相同的 LE 低位前缀，发送从首个变化字节到最高字节的后缀”生成
oracle 载荷后，两份抓包都达到 **198/198 可解释**。载荷内部仍可能穿插控制字节；
这部分是下一关。

### 13.3 对旧结论的修正

旧结论“dt49 固定 LE24 3 字节”只覆盖了绝大多数低字节发生变化的 tick。
准确描述应是：

```text
逻辑值：LE24，单位股
跨 tick：沿用前值；省略与前值相同的低位 LE 字节前缀
物理流：剩余后缀中仍可能夹入 0-2 个控制字节
```

因此正式解析器必须维护上一 tick 的字段状态；不能从每个时间戳后独立搜索一个
完整 LE24。

### 13.4 下一步（已收窄）

1. 对 `omit=0/1/3` 分组，反推时间戳/字段之间控制字节中的“省略长度”位。
2. 用两份相同真值、不同 fmt（dt49=`[0x11,0x70]` 与 `[0x70,0x88]`）的记录做
   多序列对齐：两边公共子序列是字段载荷，差异字节是控制流。
3. 控制位解出后先只实现 `dt49` 的有状态解码，并以 198 条 thsdk 真值回归；
   再利用确定的 dt49 起点反推 dt10 边界。

### 13.5 字段表附加字节是位对齐片段，不是 N（新证据）

两份同日、同票、同真值抓包的字段表对照：

```text
样本 A dt49: 31 11 70 04    dt33: 21 1a 70 04
样本 B dt49: 31 70 88 04    dt33: 21 70 d0 04
```

存在两个精确关系：

```text
ROL8(0x11, 3) = 0x88
ROL8(0x1a, 3) = 0xd0
```

并且旋转后的字节从 `0x70` 前移动到 `0x70` 后。这说明 `0x11/0x88`、
`0x1a/0xd0` 更像跨字节控制位在不同 bit cursor 下的切片，而不是“字段宽度 N”
或独立 fmt。记录区夹入字节也大量出现 `13 → 26 → 4d/9b → 37 → 6e → dc`
一类移位族，方向一致。

因此已修正 `tests/capture_auction_raw.py` 的输出文案：只称
“fmt/控制流观测值”，不再打印未经证实的 `N≈...`。下一步应把问题建模成
**有状态字段值 + 滚动控制位/位图的字节对齐**，而不是逐字段 varint。

### 13.6 688825 新股样本：控制字节还能插在时间戳 b0/b1 之间

用户对 2026-07-27 上市的科创板新股 688825 连续抓取 20 次：

```text
B 型：2880B / 旧扫描190条 / 14 次 / dt49=[70,88]
A 型：2898B / 旧扫描189条 /  6 次 / dt49=[11,70]
```

每一类型内部的响应都逐字节完全相同，说明服务端是在两个稳定编码结果中随机返回，
不是每次随机生成新控制流。A/B 的 189/190 差异也不是新股特殊业务规则：

```text
B 型 09:15:10：1e b1 66 6a ...
A 型 09:15:10：1e 43 b1 66 6a ...
```

A 型的控制字节 `0x43` 插在时间戳低字节 `b0=0x1e` 和 `b1=0xb1` 之间；
旧扫描器只允许控制字节出现在 b1 之后，所以漏掉该 tick。时间戳扫描现已兼容：

```text
[b0,b1,...marker]
[b0,ctrl,b1,...marker]
```

这进一步确认时间戳和 dt49 遭遇的是同一类滚动控制位插入，而非新股专用格式。
同一规则还找回了 603118 先前误判为“响应缺失”的 `09:18:04`、`09:24:07`；
两份 603118 稳定响应均由 198 条提升为与 thsdk 完全一致的 **200/200**。

兼容 b0/b1 插入后会多出一个 `09:25:13` 噪声候选。集合竞价 tick 的实际结束边界
是 09:25:00，因此 `_auction_ts_in_range()` 已从原先宽松的 `<=09:25:59`
收紧为 `<=09:25:00`，确定性排除该假阳性。

最终对全部新抓包回归：

```text
603118：22/22 份均解析 200 条；两份基准与 thsdk 时间戳 200/200
688825：20/20 份均解析 190 条；A/B 时间集合完全相同
```

688825 两型共同未出现的 10 个节拍为：

```text
09:15:19 09:15:37 09:15:52 09:16:10 09:17:22
09:24:16 09:24:22 09:24:28 09:24:43 09:24:49
```

当时仅凭 A/B 原始响应可确认两种编码的扫描结果一致；随后补到 thsdk 真值后，
本段结论需要修正，见 13.7。

### 13.7 688825 thsdk 真值：编码相同，但新股时间节拍不固定

通过本机已有的 thsdk 安装：

```text
C:\Users\23027\AppData\Local\hermes\hermes-agent\venv
```

成功导出：

```text
captures_live/_thsdk_auction_USHA688825_1785142871.json
```

真值共有 **201 条**：

```text
首条 09:15:01
末条 09:25:02
间隔分布：2秒×10、3秒×179、4秒×11
```

这说明 688825 没有新的字段编码格式，但“沪市严格每 3 秒、固定 ts%3”并非普遍
成立。现有解析得到 190 条，正是因为 `ts % 3` 众数过滤丢掉了 11 条非同余 tick。
原先列出的 10 个“共同缺失节拍”实际是 2/4 秒抖动后的真实时间点，并非服务端
缺记录；另有 thsdk 末条 09:25:02 在当前两份原始响应中尚未找到对应时间戳字节。

下一步时间戳判真必须从“固定模 3”升级为按物理 offset 单调、相邻时间差 2-4 秒、
时间戳结构强度联合选择；在该规则完成前，不能把 688825 的 190 条当作完整解析。

### 13.8 四票扩样本：物理编码不止 A/B 两种

用户继续抓取 600519、600276、601318、688981，各计划 8 次。去重后的结果：

| 股票 | thsdk 真值 | 原始响应数 | 唯一字节版本 | 当前时间戳结论 |
|---|---:|---:|---:|---|
| 600519 | 100 | 8 | 2 | 固定模 3 不适用；当前大量漏检/假阳性 |
| 600276 | 181 | 8 | 6 | 181 个真值全覆盖，另有 2-3 个假阳性 |
| 601318 | 161 | 初次1+修复后4 | 3 | 2/3/4 秒等混合节拍，当前大量漏检 |
| 688981 | 149 | 8 | 5 | 149 个真值全覆盖，另有 2-3 个假阳性 |

关键修正：

- “A/B 两种结果”只是最常见的两种字段表可见对齐；
- 600276 同一历史数据出现 6 个逐字节不同版本，688981 出现 5 个；
- 部分版本的控制字节穿过字段表或个股壳，导致
  `parse_field_table_fmt()` 无法识别，但响应仍是有效竞价帧；
- 正式模型必须是多相位滚动控制流，不能写 A/B 双分支。

600519 的 thsdk 时间间隔包含 2/3/4/6/9/12/15/18/21/23/24/27 秒；
601318 包含 2/3/4/5/6/7/8/9/10/12 秒。由此彻底排除“所有沪股固定每 3 秒”
作为判真规则。

### 13.9 五 FD 帧头变体修复

首次抓 601318 时，后 7 次查询均失败：

```text
invalid literal for int() with base 16: b'\xfd000007b'
invalid literal for int() with base 16: b'\xfd000007c'
```

根因是 shlv2 偶尔返回五个连续 `0xfd`。旧 `read_frame()` 命中前四字节 magic
后，把第 5 个 `0xfd` 当成 8 位 ASCII 长度的首字节。

新增 `_read_frame_body_length()`：

- 滑掉长度窗口前端多余的原始 `0xfd`；
- 补读长度字段尾部；
- 显式校验 `[0-9A-Fa-f]{8}`；
- 同时供普通 8901 和 9601 realorder 帧读取器使用。

修复后重抓 601318 连续 4/4 成功。新增
`tests/test_frame_reader.py`，覆盖标准 4FD、服务器 5FD、前导零填充三种情况。

---

## 十四、★★★ 2026-07-27 续：随机变体的真实分层与下一突破口

### 14.1 结论先行：当前缺的是 CHQuote 之前的外层归一化

对 600276、600519、601318、603118、688825、688981 六支股票的原始响应去重后，
已经观察到至少 A/B/C/D/E 五类物理形态。同一股票、同一历史竞价结果会被服务器
随机选用不同形态；同一形态重复出现时则逐字节完全一致。这不是业务数据随机变化，
也不能再建模成简单的 A/B 双分支。

精确字节 LCS 能稳定暴露版本、字段表、股票代码等共同骨架，但变体独有字节不能
当作“垃圾字节”删除：只保留共同骨架会丢掉约一半记录所需的信息。更准确的模型是：

```text
网络响应中的随机变体
  -> 尚未定位的外层省略/状态流归一化
  -> 固定布局的 CHQuote 头 + 压缩记录区
  -> 已知的 BitRLE / 位平面转置 / 字段增量解码
```

以 600276 为例，归一化后的固定头应包含：

```text
hd1.0 00
b5 00 00 00          # 181 条
3a 00                # 头长
14 00                # 每行 20 字节
05 00                # 5 个字段
01 30 00 04
0a 70 00 04
31 70 00 04
1b 70 00 04
21 70 00 04
```

原始 A/B/C 变体都能对齐到这些非零字面量，但零字节、重复值和相邻状态被不同的
控制流省略或编码了。因此“删除插入字节后直接解析”只解释了表象，不是完整算法。

### 14.2 本地二进制给出的确定证据

- `hlib.dll` 的 `CHQuoteFile` 通用 `hd1.` 分支会直接读取固定偏移：
  `DWORD[+6] & 0xffffff` 为记录数，`WORD[+0xc]` 为行宽，
  `WORD[+0xe] & 0x3fff` 为字段数，字段表从 `+0x10` 开始、每项 4 字节。
  原始 A/B/C/D/E 字节在这些偏移上均不成立，说明 CHQuoteFile 收到的已经是上游
  归一化后的缓冲区。
- 内层两条载荷路径已经明确：一种直接做位平面转置，另一种先 BitRLE 解压再做相同
  转置。仓库已有 `_decode_bitrle_0x13746d0` 和
  `_transpose_bitplane_0x1763410` 的等价实现。
- 将原始变体从多个 offset 直接按上述两种内层模式暴力尝试，对 thsdk 时间真值均为
  0 命中，进一步证明缺的不是内层参数，而是上游归一化。
- `hxsdk.dll` / `hdp.dll` 的公开 `ResponseParse` 对 8901 原始帧返回 43 且无输出，
  不能作为离线解码捷径。

`tests/reverse_auction_bitstuff.py` 现在只作为多变体共同骨架诊断器，明确不把
变体独有字节当作可删除噪声。`tests/verify_auction_corpus.py` 则把本地 thsdk JSON
作为真值，对每个逐字节唯一变体分别统计时间、价格、漏报和误报。

### 14.3 当前启发式解析器的真实基线

离线全量核对结果：

| 股票 | thsdk 条数 | 字节变体数 | 当前时间命中 | 当前价格命中概况 |
|---|---:|---:|---|---|
| 600276 | 181 | 6 | 181/181，另有 2-3 个误报 | 21-126/181 |
| 600519 | 100 | 2 | 81-82/100，另有 8-9 个误报 | 0 |
| 601318 | 161 | 3 | 104-106/161，另有 10-17 个误报 | 8-13 |
| 603118 | 200 | 3 | 200/200，无误报 | 27-111/200 |
| 688825 | 201 | 2 | 190/201，无误报 | 11-12/190 |
| 688981 | 149 | 5 | 149/149，另有 2-3 个误报 | 0 |

thsdk 真值还直接否定了“沪市固定每 3 秒一条”：600519、601318、688825 均有
2/4 秒及更长的真实间隔。生产 `_parse_auction_sh()` 中的 `ts % 3` 只能视为旧的
降噪启发式，不能作为协议事实；在完整外层解码替代它之前，不应宣称当前结果完整。

### 14.4 下一步：动态截获归一化前后缓冲区，不再盲目扩股票

当前六股语料已经覆盖不同记录数、沪主板、科创板和至少五类物理变体。继续横向抓
更多股票的边际价值很低；下一步应直接以 thsdk 真值作为逐条验收标准，并动态定位
上游转换：

1. 用 `tests/hook_auction_chquote.py` 在 `hexin.exe` 的 `CHQuoteFile` 解析入口
   （当前安装版本 RVA `0x127c8b0`；首轮记录的 `0x167c8b0` 是含 image base 的
   虚拟地址，不是 RVA）以及 `hlib.dll` 对应入口下 Hook。
2. 分别重放同一股票的 A/B/C 原始响应，在入口处导出传入缓冲区、长度和返回地址/
   调用栈。
3. 对比“网络原始变体 → 入口固定头缓冲区”，定位调用栈中最后一个改写函数；这一步
   会直接给出控制位、零省略和重复状态的真实边界。
4. 将该外层算法移植后，用 `tests/verify_auction_corpus.py` 对六股的每个唯一变体
   做时间、价格、记录数全等校验；全部通过后再替换生产启发式解析器。

换言之，现阶段最有价值的新样本不是第七支股票，而是同一份 A/B/C 网络响应各自
经过客户端归一化后的内存缓冲区。
