# hfd1.0 全市场快照专项交接（2026-08-09）

> 目标：把 hfd1.0 从「名称锚点 + THS float 扫描」升级为完整格式解码（代码压缩 +
> 数值字段），新会话从本文件开始。

## 一、你记得的「统一入口」确认

逆向 8901 时找到的 hd 家族统一入口是 **hexin.exe 的 `CHQuoteFile::parse`**：

| 项 | 值 |
|---|---|
| 反汇编虚拟地址 | `0x167c8b0`（image base `0x400000`） |
| 真实 RVA | `0x127c8b0` |
| 固定头分类/读取 | `0x136c430` |
| ABI | `thiscall parse(this, buffer, length, mode)`，被调函数 `ret 0x0c` |

`0x136c430` 识别 `hq1.`、`hq6.`、`hd1.`、`hd1.3`、`hd3.*`、`hd4.1`、`hd5.*`，
并直接读 `DWORD[+6]`、`WORD[+0xc]`、`WORD[+0xe]` 等固定头字段。出处：
`docs/plans/SH_AUCTION_X86_HARNESS_PLAN.md`。

**注意：这个分类列表里没有 `hfd1.0`。** 所以不能默认 `CHQuoteFile::parse` 会处理
hfd1.0；对照 mac dylib 有 `ParseDataHxFile` / `ParseDataHqFile` 两个入口
（`D:\code\thspy\PROTOCOL_REVERSE.md` 第七节），hfd1.0 更可能属于 HxFile 一族。

## 二、当前二进制线索

- `captures_live/hexin.loaded.bin` 和 `C:\Users\23027\AppData\Local\Temp\hexin.DMP`
  **已不在**，需要重新生成。
- 磁盘 `C:\同花顺软件\同花顺\hexin.exe` 是 UPX 包，直接搜字符串只有 `hd3.1`
  （file offset `0xc56d87`），**没有 `hfd1.0` / `hd1.0`**。
- 找 hfd1.0 解析入口的第一步：任务管理器抓 DMP →
  `tests/reverse_hexin_minidump.py extract-module` →
  `tests/reverse_hexin_loaded.py --string hfd1.0`，再 `--xref-rva` 找引用。

## 三、现有实现与语料

当前生产路径：

- `src/thspypc/parse_hfd1.py`：名称锚点 + oracle 映射 + 名称后 THS float 扫描，
  数值字段明确标注「实验性，不保证准确」。
- `src/thspypc/services/market_snapshot.py` + `src/thspypc/features/snapshot_protocol.py`：
  MAIN 空括号单请求；只解析第一条含 `hfd1.0` 的帧。
- `market_snapshot_with_quotes()` 已不走 hfd1.0，改为 stock_list + list_quotes
  混合兜底，因此 hfd1.0 现在是「格式完成度」问题，不是功能阻断。

本仓库语料（`data/`）：

- `hfd1_0_response.bin`（326KB 原始响应）
- `hfd1_0_name_anchors.json`（1209 个可靠锚点）
- `oracle_code_names.json`（明文 code+name GBK 对照）

更全语料在 `D:\code\ths_takehome\thspypc\data\`：

- `hfd1_0_boundaries.json`（约 2.4 万条扫描边界，含噪声需过滤）
- `hfd1_0_parsed.json`
- `snapshot_沪市_16-22.bin`、`snapshot_深市_144-151.bin`、`snapshot_沪16_深32.bin`

最接近的已知 codec：`D:\code\thspy\protocol.py` 的 `_parse_one_record_200` /
`_read_varlen_field` / `_fill_numeric_fields_200` / `_parse_hx200_mapped`
（id=200 基础数据，PC hfd1.0 是它的 PC 变体）。

## 四、已确认事实

- hfd1.0 与 mac id=200 共享 LZ 压缩原理：代码区控制字节压缩（PC 用 `0x07`，
  mac 用 `0x01`），名称 GBK 明文，数值区变长（THS float + `01 XX YY` 去重标记思路）。
- PC 版没有 mac 的 `fe0100` 锚点和 `05800000` 尾标记，控制字节也不同，不能直接复用。
- 记录边界特征：`[末位数字 ASCII + GBK 名称]`；`pre[-1] == code[-1]` 约 85%。
- 覆盖：市场 16（沪 A）、144-151（新三板/基金/北交所）；**深市 A 股（32/33）不支持**。
- 数值字段扫描不可靠：THS float 编码过宽，几乎所有 4B 都能解出「合理」值，
  字段偏移按记录类型漂移。必须用 oracle 逐条对照或动态逆向。

## 五、新会话路线（按代价从低到高）

### 1. 先跑离线基线

```bash
uv run python tests/analyze_hfd1_0.py
uv run python tests/analyze_hfd1_compression.py
uv run python tests/analyze_hfd1_raw_layout.py
uv run python tests/calibrate_hfd1_numeric.py
uv run python -m thspypc.parse_hfd1
```

### 2. 路径 A：纯语料 + mac codec 移植

把 `_parse_one_record_200` 的变长字段思路移植到 PC 0x07 变体：

1. 用名称末位数字锚点切记录边界（已有 `hfd1_0_boundaries.json`）；
2. 从名称结束后用「价格 + 涨速」双字段验证定位数值区起点
   （参考 `_locate_price_200`，比单价格扫描稳）；
3. 用 list_quotes oracle（价格/涨跌幅/量/额）逐条校准字段偏移和去重标记；
4. 指数 vs 股票分别建模（数值区布局不同）。

### 3. 路径 B：重新拿加载映像，找 hfd1.0 解析入口

1. 任务管理器导出 hexin DMP；
2. `uv run python tests/reverse_hexin_minidump.py extract-module ...` 生成
   `hexin.loaded.bin`；
3. `uv run python tests/reverse_hexin_loaded.py --image ... --string hfd1.0` 找字符串；
4. 找到字符串引用后 `--xref-rva` 定位解析函数，参考
   `tests/emulate_chquote_parse.py` 的 Unicorn harness 提取纯函数；
5. 记住 playbook 的教训：先确认输入是否还在已知外层（`normalize_8901_response`），
   新抓的响应如果以 `0x0a` 开头，先 normalize 再谈记录结构。

### 4. 活网补语料时的登录规则

按 `AGENTS.md`：不要手写串行登录、不要重复 connect。hfd1.0 只有部分 host 支持
（历史实测 122.9.202.190 可用），需要低层 socket 时用
`login_socket_for_domains()`，认证材料从同一个 `get_client()` 的 AuthService 取。

## 六、验收标准

- (code, name) 覆盖不低于现有 1209 锚点；
- 价格/涨跌幅/最高/最低/开盘/量/额与 `list_quotes` 真值逐条对齐；
- 指数与股票分开建模且都能对齐；
- 深市继续由 `market_snapshot_with_quotes()` 混合方案兜底；
- 现有 `uv run pytest -q` 不回归。

## 七、相关命令速查

```bash
# 离线分析
uv run python tests/analyze_hfd1_0.py
uv run python tests/analyze_hfd1_compression.py
uv run python tests/analyze_hfd1_numeric.py
uv run python tests/analyze_hfd1_numeric_v2.py
uv run python tests/analyze_hfd1_raw_layout.py
uv run python tests/analyze_hfd1_scanrecords.py
uv run python tests/expand_hfd1_anchors.py
uv run python tests/calibrate_hfd1_numeric.py

# 活网采集（仅需要新语料时）
uv run python tests/collect_snapshot_oracle.py

# 静态 RE（需要重新生成的 hexin.loaded.bin）
uv run python tests/reverse_hexin_loaded.py --image captures_live/hexin.loaded.bin --string hfd1.0
uv run python tests/reverse_hexin_loaded.py --image captures_live/hexin.loaded.bin --xref-rva 0x...
```