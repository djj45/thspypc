# 看盘主界面抓包交接 2026-08-05

## 背景

2026-08-04 ~ 2026-08-05 对同花顺 PC 客户端「看盘主界面」做了多轮抓包，
对照 thspypc 现有实现，确认了**真实客户端的 pageid/period/route 体系与代码假设的差异**，
并发现了若干未实现协议。本文档记录抓包结论、与 `PROTOCOL_AND_IMPLEMENTATION_GUIDE.md`
的逐项对比、以及据此要做的实现修正。

抓包工具：`tests/capture_kanpan.py`（含「已知协议识别器」，本次抓包后据此迭代修正了
识别器的两处误报：① route 是会话内动态实例号、不该按固定集合判未知；② 9601 纯文本帧
按 `method=` 归类而非「无 pageid 新协议」）。

## 抓包文件清单（captures_live/）

| 文件 | 账号 | 大小 | 说明 |
|---|---|---|---|
| `kanpan_20260804_235612.pcap` | **Level2** | 2.9MB | 看盘界面全流量（板块/分时/盘口/短线精灵翻页）；首次发现 9601 statscalc/calcext |
| `kanpan_20260805_000650.pcap` | **Level2** | 216KB | 点 3 只股票的分时/日K/盘口；发现 pageid=1334 + 未知 period 7169/7173/7174/4096 |
| `kanpan_20260805_001523.pcap` | **Level2** | 597KB | 进入分时/日K界面（浙江荣泰 603118）；确认 1334 做分时、4214/4417 做 L2 增强 |
| `kanpan_20260805_003410.pcap` | **普通** | 1.4MB | 普通账号分时/日K/盘口；确认普通账号用 9354/9355/1335（与代码一致） |
| `kanpan_20260805_003548.pcap` | **普通** | 1.6MB | 普通账号板块加载；确认 392 + route 0x0039/0x0139（与代码一致） |

## 结论速览

### 1. pageid 体系：Level2 与普通账号路径不同，代码的 9354/9355 是普通账号路径

**这是最重要的结论。** 同花顺客户端按账号类型走完全不同的 pageid：

| 业务 | 普通账号（代码现状，抓包确认✅） | Level2 账号（抓包真实，代码不一致❌） |
|---|---|---|
| 当日分时 | `9354` period=8192 route=0x010A | `1334` period=8192 route=0x0133/0x0033 |
| 日K线 | `9355` period=16384 route=0x0001/0x013B | `1334` period=16384 route=0x0100/0x0033 |
| 历史分时 | `9355` period=8192 route=0x005D | `4417` period=16384/8192（L2通道） |
| 列表行情/盘口快照 | `1335` period=0 多种route | `1334` period=0 多种route |
| 五档盘口 | `1335` DataType=24,25,30,31 | `1334` DataType=13,18,24,25... |
| 早盘竞价 | `9354`/`9355` period=7176/6144 | `1334` period=7176 route=0x01FC |
| 尾盘竞价 | `9354`/`9355` period=7424 | `1334` period=7424 route=0x0100 |
| 板块列表 | `392` route=0x0039/0x0139 | `1334` route=0x002B/0x012B（8901） |
| L2增强分时 | —（普通账号无） | `4214` period=8192 route=0x0201 |
| L2增强历史/K线 | — | `4417` period=16384/8192 |

**要点**：
- thspypc 代码里的 `9354`(分时)/`9355`(K线/历史) 是**普通账号在 MAIN 通道上验证可用的 pageid**，
  抓包确认真实客户端普通账号确实用这两个值——**代码正确，不要改普通账号路径**。
- Level2 账号在真实客户端里**不用 9354/9355**，而是统一用 `1334`（走 L2 连接 stream）做
  分时/K线/盘口/竞价，再用 `4214/4417` 做 L2 增强。thspypc 当前让 Level2 也走 9354/9355，
  虽然服务器能返回数据（pageid 不严格校验），但与真实客户端不一致。
- `1334` 在普通账号是**排行榜**（stock_list，DataType=199112），在 Level2 账号是**通用看盘请求**
  pageid——同一个数字在不同账号下语义不同，这是同花顺的设计。

### 2. 盘口区新 period（Level2，pageid=4214/4417，未实现）

抓包发现 Level2 账号在盘口/逐笔区用了 4 个 thspypc 未实现的 period：

| period | pageid | DataType | 推断业务 | 状态 |
|---|---|---|---|---|
| **7169** | 4214 | 10,12,13 | 逐笔成交回放（全天逐笔） | ❌ 路线图缺口。注意真实 pageid 是 **4214**，不是 HANDOFF_SUPERORDER 推测的 4260 |
| **7173** | 4417 | 10 | 逐笔委托？（待确认） | ❌ 完全未知 |
| **7174** | 4214 | 10 | 逐笔相关（待确认） | ❌ 完全未知 |
| **4096** | 4417 | 7,49,12,18,75,10 | 分笔 tick（个股，类比指数分笔） | ❌ 未实现 |

> 这组协议复杂（逐笔回放单帧 440KB+），本轮**暂不实现**，仅记录。

### 3. 9601 板块统计协议（statscalc/calcext，✅ 已实现 2026-08-05）

真实客户端的板块列表除 8901 的 392/1334 请求外，还走 9601 的**纯文本计算协议**。
两者共享帧封装 ``\x09`` + ``\n`` 分隔 GBK key=value 文本 + ``\x00``（请求/响应
同构，无二进制子帧头）：

| method | 节点 | 请求格式 | 返回 | 用途 |
|---|---|---|---|---|
| `statscalc` | 独立统计节点 `8.132.233.77:9601`（不在 DNS/passport） | `instid= method=statscalc market=48, codelist=48(881101,...) datatype=330342 dataclass=intervalcalc interval=0-0 rightstype=forward period=0 datetime=0(0-0) rettype=hdfile` | hd1.0 表（每码 24B：code8+pad8+date4LE+value4float） | 板块批量统计（涨跌幅/涨速/资金流聚合计算） |
| `calcext` | REALORDER 节点 `106.14.65.90:9601`（与 qurealorder 共享） | `instid= method=calcext codelist=33(003017,) datatype=199359 rightstype=forward rettype=json` | JSON `{"data":[{"3":<market>,"4":"<code>","199359":<value>}]}` | 单板块/个股扩展计算 |

实测数据：
- `statscalc` `dataclass=updownlimit datatype=330326,330328` → 涨跌停统计
- `calcext` 对单只股票（如 003017/003018）请求 `datatype=199359 rettype=json`
- 端口：全部 9601（与短线精灵同端口，但走独立 instid 流）
- thspypc 的 `board_quotes` 走 8901 fu4 通道（pageid 392/5716），是另一套协议，功能可达但路径不同

**thspypc 实现**（2026-08-05）：
- `features/board_stats_protocol.py`：请求构造（`build_statscalc_query` /
  `build_calcext_query`，字段顺序逐字节对齐抓包）+ 响应解析
  （`parse_statscalc_response` / `parse_calcext_response`，record_len 由 payload
  计算不写死 24）。
- `services/board_stats.py` `BoardStatsService`：statscalc 走
  `ConnectionRole.BOARD_STATS`（独立统计节点，懒连接 + 心跳），calcext 走
  `ConnectionRole.REALORDER`（复用 9601 socket，门控用 `Capability.REALORDER`
  与短线精灵一致，REALORDER 能力证据在首次 9601 建连时懒建立）。
- 公开 API：`board_stats_interval` / `board_stats_updownlimit` / `board_calcext`。
  statscalc 节点不可达时优雅降级返回空列表（可降级 `board_quotes`），不影响 calcext。
- 节点 IP 用 `THSPYPC_STATSCALC_HOST` 环境变量覆盖。

**活网验证结论**（2026-08-05）：
- ✅ statscalc：`board_stats_interval(["881121"])` 返回 `{code:0881121, date:20160127, value:-3.19}`，hd1.0 解析正确。
- ✅ calcext：`board_calcext("600030", 17)` 返回流通市值 `4054 亿`，JSON 解析正确。
- ⚠ **statscalc 是低频计算协议**：抓包确认真实客户端请求间隔 **~9 秒**（服务端区间聚合
  计算耗时）。连续高频调用（如循环批量查）会被服务端限流/丢弃导致超时。正确用法是
  低频轮询（≥10s 间隔），不要像行情查询那样连续发。首次请求还需预留建连时间
  （PC login 到独立统计节点 ~15s）。
- calcext 与 qurealorder 共享 REALORDER socket，无此限流问题，可连续调用。

**与 8901 board_quotes 的区别**：statscalc 是**服务端计算型**（`dataclass=intervalcalc` 让
服务器做区间聚合），8901 board_quotes 只取预存字段。两者可互补、交叉验证。

### 4. 短线精灵翻页（qurealorder）：基本对齐，3 处差异

抓包（Level2 账号，勾选 4 类异动：大笔买入/卖出、打开涨停/跌停）对照 thspypc `build_qurealorder_query`：

| 参数 | thspypc 代码 | 真实客户端 | 对齐 |
|---|---|---|---|
| `method=qurealorder` / `reqtype=4` / `rettype=hqfile` | ✅ | ✅ | ✅ |
| `maxcount` | 写死 `80` | **80 / 120 / 1000 三种**（见下） | ⚠️ 需改成按市场可选 |
| `datatype` | `DXJL_DATATYPE`（4类） | 4类（与勾选一致）| ✅ 本次勾选对齐 |
| `accept_ziptype=snappy` | ❌ 未发 | ✅ **46/46 帧全发** | ⚠️ 需补 |
| 响应压缩 | — | **实测未压缩**（明文 hq1.0，record_count=80 record_len=45，数据区可见明文代码 300750） | ✅ 不修也能用 |

**maxcount 三种值的含义**（实测 market 分布）：

| maxcount | 适用 market | 含义 |
|---|---|---|
| **80** | 16(沪)/32(深)/151(北交所)/48(板块) | 个股市场标准每页条数（异动多） |
| **120** | 151(北交所)/16/32/48 | 北交所等异动较少市场的翻页条数 |
| **1000** | 48(板块)/16(沪首批) | 板块异动少，一次拉满；或首批全量加载 |

> 同花顺客户端按市场/场景动态选 maxcount；thspypc 写死 80 在个股市场可用，但板块/北交所
> 场景分页不合理。`accept_ziptype=snappy` 盘后小响应未压缩，盘中大响应可能压缩（待验证）。

## 与 PROTOCOL_AND_IMPLEMENTATION_GUIDE.md 的逐项对比

### 第 6 节「业务请求速查总表」需修订

文档现表（普通账号为主）抓包确认✅，但**缺少 Level2 账号的 pageid 对照**。补充：

| 业务 | 文档现状（普通） | Level2 真实（抓包新增） |
|---|---|---|
| 当日分时 | 9354 period=8192 | **1334** period=8192 route=0x0133/0x0033（+4214 L2增强） |
| 日K | 9355 period=16384 | **1334** period=16384 route=0x0100/0x0033 |
| 历史分时 | 9355 period=8192 | **4417** period=16384/8192（L2通道） |
| 五档盘口 | 1335 DataType=24,25... | **1334** DataType=13,18,24,25... |
| 早盘竞价 | 9354 period=7176 | **1334** period=7176 route=0x01FC |
| 尾盘竞价 | 9354 period=7424 | **1334** period=7424 route=0x0100 |
| 板块列表 | 392 route=0x0039 | **1334** route=0x002B/0x012B |

### 第 12 节「K线」补充

文档说 K线「始终走 MAIN，pageid=9355」——这只对**普通账号**成立。Level2 账号真实客户端用：
- `pageid=1334` period=16384 route=0x0100（普通日K，走L2连接）
- `pageid=4417` period=16384 route=0x0100/0x0157（L2增强日K，带大单字段 207/206/205...）
- `pageid=4214` period=16384 route=0x0100（L2通道日K，DataType=13,407）

### 新增「盘口逐笔协议」小节（待实现）

| period | pageid | DataType | 业务 | 参考 |
|---|---|---|---|---|
| 7169 | 4214 | 10,12,13 | 逐笔成交回放 | HANDOFF_SUPERORDER_20260726（pageid 需更正为4214） |
| 7173 | 4417 | 10 | 逐笔委托？（待确认） | — |
| 7174 | 4214 | 10 | 逐笔相关（待确认） | — |
| 4096 | 4417 | 7,49,12,18,75,10 | 分笔 tick | 类比指数分笔 period=4096 |

## 本轮实现修正（已确认要做）

### A. 短线精灵：补 accept_ziptype=snappy + maxcount 参数化

**文件**：`src/thspypc/features/realorder_protocol.py`

1. `build_qurealorder_query` 增加参数 `accept_ziptype: str = "snappy"`，文本末尾在
   `rettype=hqfile` 前插入 `accept_ziptype=snappy\n`（对齐真实客户端 46/46 帧）。
2. `maxcount` 从写死 80 改为参数（默认仍 80，保持兼容），并在 docstring 写明三种值的含义。
3. `services/realorder.py` 的 `dxjl_page` 透传 maxcount（已有参数，确认默认值与文档一致）。

> 实测响应未压缩（明文 hq1.0），所以 `accept_ziptype=snappy` 不影响解析，仅对齐请求形态。
> 解析器无需改。

### B. Level2 pageid 对齐 1334（已实现，2026-08-05）

经抓包确认并实现：
- **普通账号**（9354/9355/1335）路径与真实客户端一致，**保持不变**。
- **Level2 账号**的分时/K线/当日竞价主体 pageid 从 4214 改为 **1334**（DataType/route 不变），
  对齐真实客户端。关键改动：

| 业务 | 文件 | 改动 |
|---|---|---|
| 分时（当日） | `features/timeline_protocol.py` `build_timeline_l2_query` | 新增 `pageid` 参数（默认4214向后兼容）；`services/timeline.py` `build_timeline_request` L2 分支传 `pageid=1334` |
| 日K/周K/分钟K | `features/kline_protocol.py` | 新增 `build_kline_l2_query`（pageid=1334, route=0x0100）；`build_kline_query` 新增 `route` 参数 |
| K线服务层 | `services/kline.py` | 新增 profile 判断：Level2 走 L2 连接(SH_L2/SZ_L2)+1334，普通走 MAIN+9355 |
| K线门面 | `_client/service_facade.py` | 按 profile 选 capability（BASIC_QUOTE / L2_TIMELINE） |
| 当日开盘竞价 | `features/auction_protocol.py` `build_auction_query` | pageid 4214→1334，route 0x01FC 不变 |
| 当日尾盘竞价 | `features/auction_protocol.py` `build_l2_closing_auction_query` | 当日 pageid 4214→1334；历史保持 4417 |

**关键确认**：1334 分时请求的 DataType 仍含 dt223-230 大单字段（抓包帧15/96 确认），
改 pageid **不丢失 L2 大单曲线**。解析器全部不依赖 pageid（靠 flag/record_size 区分），无需改。

**不改的部分**（有 fixture 背书或功能可用）：
- 历史分时 `build_history_timeline_query`（4417，有 fixture）
- 历史竞价 `build_l2_history_auction_query`（4417，抓包确认）
- 盘口十档 `build_depth_ten_query`（4214，有 fixture `req_000938_4214_ten.bin`）
- 盘口五档 `build_depth_quote_query`（1333，走 MAIN，功能可用）

> ⚠️ 待活网验证：1334 路径虽对齐了请求形态，但响应是否仍为 flag=0x00B4（L2表）需 Level2 账号
> 活网确认。若响应 flag 变化，需调整解析器。测试已全部更新（388 passed）。

### C. 盘口新协议（7169/7173/4096）：本轮不实现

逐笔回放协议复杂（单帧 440KB+，变长字段，HANDOFF_SUPERORDER 的 fmt 子标记 22/34 未破译），
且 HANDOFF_SUPERORDER 推测的 pageid=4260 与本次抓包（pageid=4214 period=7169）不符，需
重新逆向。本轮仅记录，留作后续 P2-R&D。

请求形态已确认（2026-08-05 抓包 `kanpan_20260805_010356`）：

```
# 逐笔成交回放
CodeList=33(000938,); DataType=10,12,13, DateTime=7169(-27-0) pageid=4214  route=0x02FC
# 逐笔委托
CodeList=33(000938,); DataType=10, DateTime=7173(-1-0) pageid=4214  route=0x02FC
```

### D. 竞价 trade_date 默认值修复（盘后超时根因）

**问题**：`closing_auction()` / `auction()` 不传 `trade_date` 时原用 `date.today()`，
盘后/非交易日请求"当日"竞价导致超时（当日无数据）。2026-08-05 抓包确认同花顺客户端
盘后查竞价用的是**最近交易日**（8/4）的显式时间戳（`7424(1785826620-1785826800)`
= 8/4 14:57-15:00），而非"今天"。

**修复**：`auction_protocol.py` 新增 `resolve_trade_date(value)`：
- `None` → 最近已收盘交易日（周末回退；工作日 15:00 前回退到前一交易日）
- 显式日期 → 原样返回
- 三处 `trade_date or datetime.now().date()` 和 `build_auction_query` 的 `0-0` 默认
  全部替换

**活网验证（盘后 2026-08-05）**：

| 接口 | 修复前 | 修复后 |
|---|---|---|
| `closing_auction()` 不传日期 | ⏱ 超时 | ✅ 21 tick（8/4 14:57-15:00） |
| `auction()` 不传日期 | ✅（`0-0` 服务器自决） | ✅ 68 tick（8/4 09:15-09:24） |

### E. 1334 改动活网验证结论

Level2 账号（2026-08-05 盘后）全部通过：

| 接口 | pageid | 结果 |
|---|---|---|
| 分时 timeline | 1334 | ✅ 241 点 + L2 大单字段 dt227/229（不丢失） |
| 日K kline | 1334 + SH_L2/SZ_L2 | ✅ 11 根（新 L2 路径打通） |
| 周K kline | 1334 | ✅ 6 根 |
| 当日竞价 auction | 1334 | ✅ 68 tick |
| 当日尾盘 closing_auction | 1334 | ✅ 21 tick（修 trade_date 后） |
| 历史尾盘（4417 未改） | 4417 | ✅ 21 tick |

**关键确认**：1334 分时请求的 DataType 仍含 dt223-230 大单字段，改 pageid 不丢失
L2 大单曲线；响应仍为 flag=0x00B4 的 L2 表，解析器无需改。

### F. 看盘界面指数实时推送（✅ 已实现 2026-08-05）

盘中抓包（``captures_live/kanpan_push_20260805_132347.pcap`` +
``index_push_20260805_140642.pcapng``）发现看盘界面有一类 ``09 7b d0 0f`` 头的
实时推送帧（之前归在"其他"里），是 pageid=5716 subreal 订阅的服务端推送。

**五大指数全局推送**：客户端启动时一次性注册（pageid=5716 + PushField=16:241;32:241
+ subreal URS/UCT/UNX/UCX/UME），服务端持续推送指数点位。

**字段解码**（4 字节 LE THS-float，深市/沪市字段顺序一致，起点偏移不同；北证50
紧凑帧用相对代码偏移，与分时响应交叉验证逐字节确认）：

| 字段 | 深市(399xxx) | 沪市(1Axxxx) | 北证(899050) |
|---|---|---|---|
| dt6 昨收 | off=48 | off=39 | — |
| dt7 开盘 | off=52 | off=43 | — |
| 最高 | off=56 | off=47 | — |
| 最低 | off=60 | off=51 | — |
| dt10 最新 | off=64 | off=55 | code+6 |
| dt19 成交额 | off=76 | off=63 | code+14 |

**活网验证**（2026-08-05 盘中）：399001=14129/+1.76%、1A0001=3872/+1.32%、
1B0680=1928/+4.72%、899050=1114。

**实现**：``features/index_push_protocol.py`` 的 ``parse_index_push``，支持三套
布局（深市/沪市/北证），9 个测试（含真实帧）全通过。

### G. 北证50 分时解析兼容（✅ 已修复 2026-08-05）

北证50（899050）当日分时返回 ``flag=0x0046`` hd3.1 表（230 点，无 dt40），现有
``parse_timeline_response`` 拒绝（只接受指数 flag 0x3E/0x86/0x9E + dt40，或个股
record_count==241）导致超时。**这不是权限问题，是解析器兼容问题**。

修复：``timeline_protocol.py`` 个股分时路径放宽 record_count（200-242）+ shell
市场标记扩展（0x11/0x12/0x13/0x21/0x25）。

## 抓包方法学补充（给 capture_kanpan.py 的后续维护）

本次抓包迭代修正了识别器两处设计错误，已写入 `tests/capture_kanpan.py`：

1. **route 不是协议常量**：route 是会话内动态分配的「页面组件实例号」，同一业务每次连接
   route 不同（如分时 route 在不同 stream 是 0x0133/0x0033/0x013C）。识别器原先按固定
   `KNOWN_ROUTES` 集合判「未知route」造成海量误报。修正：仅对「未知 pageid/协议」才提示
   route，已知业务不判 route。

2. **9601 纯文本帧按 method= 归类**：statscalc/calcext/qurealorder 等是 `\x09` + `instid=`/`method=`
   纯文本帧，不走 pageid 体系。识别器原先归「无pageid新协议」，修正为按 `method=` 关键词
   分类（statscalc→板块统计、calcext→板块扩展、qurealorder→短线精灵历史）。

## H. 盘口逐笔协议（7169/7173/7174/4096）——秒表对齐抓包定论（2026-08-05 盘后）

> 两轮 `capture_superorder.py` 抓包（盘后，深市 000938 紫光股份）+ **秒表逐操作对齐**，
> 把 §2 原本标注「完全未知/待确认」的四个 period 的**触发时机**钉死，并修正了
> `PROTOCOL_AND_IMPLEMENTATION_GUIDE.md` §17.1 的若干误判。响应字段破译待续
> （样本已导出 `superorder_20260805_195859_resp_stream1.bin`）。

### H.1 两轮抓包（同账号同股票，秒表对齐）

| 抓包 | 大小 | 操作流程（秒表） |
|---|---|---|
| `superorder_20260805_195211.pcap`（15.3MB） | 第一轮，无秒表 | 冷启动→看盘→000938→分时→逐笔翻页→超级盘口 |
| `superorder_20260805_195859.pcap`（16.5MB） | ★第二轮，带秒表 | 0-10s 冷启动 / 10-20s 看盘 / 20-30s 打开000938 / 30-40s 分时 / 40-60s 误操作 / 60-67s 逐笔翻页 / 随后超级盘口 |

两轮请求模式**逐条吻合**（仅第一轮 41.8s 多一条 4214 日K，确认是误触发的偶发，非协议本身）。
第二轮秒表对齐验证了每个 UI 操作对应的请求，**协议判断的可信度由此升级**。

### H.2 UI 操作 → 协议请求对应表（秒表钉死）

| UI 操作 | 抓包时刻 | 触发的请求 |
|---|---|---|
| 冷启动进看盘 | 0.7s | pageid=5716 基础订阅 |
| 打开看盘 / 打开股票 | 16.1s | pageid=1334 全套（**含 16384 日K，默认加载**，非用户主动切） |
| 进入 L2 分时通道 | 24.5s | pageid=4214 分时全套 |
| 进入逐笔成交 | 24.6s | `4214 DT=7173(-1) DT=7169(-27)` 初始 |
| **逐笔成交翻页** | 51.7-57.1s | `4214 DT=7169(1785913200→1785912654)` **区间从 15:00 往前滚** ★ |
| 逐笔误触 | 47.0s | `4214 DT=7174(-1) + 7169(-26)` |
| **打开超级盘口** | 62.9s | **pageid=4260 全套**（7174/7173/7169/4096/7424/7176 成组） |
| 超级盘口浏览 | 63.4-64.8s | `4260 DT=7169` 区间 **9:33→13:01 全天** |

**unix 时间戳全部落在真实交易时段**（9:33–15:00），坐实 7169 是历史回放型协议。

### H.3 四个 period 的修正结论

| period | §2 旧标注 | 秒表对齐新证据 | 修正后结论 |
|---|---|---|---|
| **7169** | 逐笔成交回放（pageid 4260→4214） | ✅ 翻页时区间滚动铁证 | **逐笔成交/超级盘口历史回放**。**双 pageid 都用**：4214（逐笔面板，区间滚动）+ 4260（超级盘口，全天浏览）。§17.1「真实 pageid 是 4214 非 4260」**此判断错误**，两个都在用 |
| **7173** | 逐笔委托？（待确认） | 进入逐笔 + 打开超级盘口时**成组触发**，`0-0`/`-1` 非历史区间 | **逐笔/超级盘口配套短查询**（DataType 仅 10）。"逐笔委托"标签无响应内容证据，降级为「配套短查询」 |
| **7174** | 偶发（未复现） | **本轮复现 2 次**（47.0s 误触 + 62.9s 超级盘口），与 7173 成对 | **同 7173，超级盘口/逐笔配套短查询**，非偶发 |
| **4096** | 分笔 tick（类比指数分笔） | 4214 通道夹在逐笔翻页中间触发 `(-202-<unix>)`；4260 通道打开超级盘口时触发 `(0-0)` | **双形态**：4214（DataType 7,49,12,18,75,10,6,66）vs 4260（20+字段）。"分笔 tick"类比合理但**两通道业务/字段不同**，未证实 |

### H.4 修正 §17.1 的三处误判

1. **「真实 pageid 是 4214，非 HANDOFF_SUPERORDER 推测的 4260」** → **错**。两轮抓包证明
   **4214（逐笔面板）和 4260（超级盘口）都用 7169**，是两个 UI 入口共用同一 period。
2. **「7174 偶发」** → **错**。两轮都复现，与 7173 成对出现在进入逐笔 / 打开超级盘口时。
3. **16384 日K**：与逐笔/超级盘口无关，是**打开股票时看盘页面默认加载的日K组件**，
   无需用户主动切换。§17.1 没说错但易误解，特此澄清。

### H.5 关键样本文件

- `captures_live/superorder_20260805_195859.pcap`（16.5MB）+ `_resp_stream1.bin`（13.5MB，172 帧）— ★秒表对齐、深市 000938
- `captures_live/superorder_20260805_195211.pcap`（15.3MB）+ `_resp_stream1.bin`（12.2MB）— 第一轮对照
- 两份响应流含 7169 的 440KB+ 帧，**深市样本**（之前 HANDOFF_SUPERORDER 只有沪市 603118），深沪对照可攻字段表破译

### H.6 待破译（下一步）

- **7169 响应字段表**：`HANDOFF_SUPERORDER_20260726` 卡在 fmt 子标记（0x11/0x1a/0x22/0x34）的变长编码。
  本轮新增深市 000938 样本，与沪市 603118 对照可能突破。
- **7173/7174 业务**：仅有触发时机，无响应内容。"逐笔委托"标签待响应破译后才能定论。
- **4096 双形态**：4214 与 4260 通道的 DataType 差异巨大，业务是否相同未证实。

### H.7 7169 深市响应帧破译进展（2026-08-05 盘后，初版）

对 `superorder_20260805_195859_resp_stream1.bin` 最大帧（2.5MB，对应 `4260 DT=7169(1785907131-1785913200)` = 13:18:51–15:00:00 的超级盘口请求）做破译：

**帧结构**（cmd=0x0a 外层压缩，需先 `normalize_8901_response`）：
```
正规化前 2,547,841 B → 正规化后 4,961,189 B
[帧头] CodeListSize~=1 ...
[hd1.0] @ off=127 (标准 hd1.0，非沪市 hd\x8d1.0 变体)
[hd1.0 头] marker+6: record_count(LE32)=155031, flag=0x0046, hs(行宽)=32, fc(字段数)=8
[字段表] 8 项 × 4 字节:
  dt1(fmt=0x30,w=4) dt56(0x30,4) dt10(0x70,4) dt13(0x70,4)
  dt12(0x30,4) dt74(0x30,4) dt75(0x30,4) dt18(0x70,4)
[个股壳] 0x21 + "000938" (深市标记 0x21，非沪市 0x11)
[数据区] 155031 × 32 = 4,960,992 字节 (行主序)
```

**已铁证确认的字段**（行主序直接读取，**非** BitRLE/位平面转置——虽 flag=0x0046 但实测行主序可读）：

| 字节偏移 | 类型 | 字段 | 验证 |
|---|---|---|---|
| +14 | LE32 | **时间戳**（dt1） | 13:18:51→15:00:00，与请求区间 1785907131 起点吻合，间距均匀 |
| +18 | LE32 ths_float | **成交价**（dt10） | 36.92–37.75 元，紫光股份 000938 当日合理股价区间 |
| +22 | LE16 | **成交量**（dt12，手） | 100/200/400/500/7400，逐笔成交量合理 |

**待破译的字段**（值不稳定或语义未定）：
- +0/+4/+8/+12：递增序列，疑为成交号/序号（dt56/dt74/dt75 之一）
- +28 ths_float：值不稳定（含负数），**非累计成交额**，语义待定
- +26 byte：取值 {1,5}，疑为买卖方向/成交标志

**关键差异 vs 沪市（HANDOFF_SUPERORDER 603118）**：
- 沪市：`hd\x8d1.0` 变体标记 + 440KB + 字段表 29 项 + **变长/参数化编码**（fmt 子标记 0x11/0x1a 未破）
- 深市：标准 `hd1.0` + 4.96MB(正规化后) + 字段表 8 项 + **行主序定长 32B/记录**（直接可读！）
- **深市比沪市简单得多**——定长行主序，无变长编码。深市路径可作为**首个可实现的 7169 解析器**。

**⚠ flag=0x0046 的处理**：现有 `timeline_protocol.py` 对 0x0046 走 BitRLE+位平面转置路径，
但 7169 这帧实测是行主序（时间戳/价/量直接可读）。可能 0x0046 在不同业务（分时 vs 逐笔）含义不同，
或 7169 逐笔帧不走位平面。**实现时不能复用 timeline 的 0x0046 分支**，需独立判定。

**下一步**：
1. 确认其余 5 个字段的字节偏移与语义（dt13 成交额 / dt56 序号 / dt74,75 / dt18 / 买卖方向）
2. 验证 4214 通道（逐笔面板，非超级盘口）的 7169 响应是否同构
3. 若确认深市定长行主序稳定，可优先实现深市 7169 解析器（沪市变长留作后续）

### H.8 7169 深市响应帧完整字段破译（2026-08-05 盘后）

> 承接 §H.7。**记录起点校正**为 `new_off = 201`（时间戳作为 dt1 在记录开头，非 §H.7
> 误判的 187）。8 个字段全部定位，6 个语义铁证确认，2 个累计字段语义待 thsdk 交叉验证。

**帧结构**（cmd=0x0a 外层压缩，需先 `normalize_8901_response`）：
```
正规化前 2,547,841 B → 正规化后 4,961,189 B
[帧头] CodeListSize~=1 ...
[hd1.0] @ off=127（标准 hd1.0，非沪市 hd\x8d1.0 变体）
[hd1.0 头] marker+6 (=133):
  record_count(LE32)=155031, flag=0x0046, hs(行宽)=32, fc(字段数)=8
[字段表] base+10 (=143), 8 项 × 4 字节:
  dt1(0x30,4) dt56(0x30,4) dt10(0x70,4) dt13(0x70,4)
  dt12(0x30,4) dt74(0x30,4) dt75(0x30,4) dt18(0x70,4)
[个股壳] 0x21 + "000938"（深市标记 0x21，非沪市 0x11）
[数据区] @ off=201, 155031 × 32 = 4,960,992 字节（行主序定长）
```

**完整字段布局**（记录起点 off=201，hs=32，行主序按字段表顺序）：

| 记录内偏移 | dt | fmt | 类型 | 语义 | 验证 |
|---|---|---|---|---|---|
| +0 | dt1 | 0x30 | LE32 | **时间戳**（unix 秒） | 13:18:51→15:00:00，与请求区间起点 1785907131 吻合 ✅ |
| +4 | dt56 | 0x30 | LE32 ths_float | **成交价**（元） | 36.92–37.75，紫光 000938 当日合理区间 ✅ |
| +8 | dt10 | 0x70 | LE32 | **成交量**（手） | 100/200/400/500/7400，逐笔量合理 ✅ |
| +12 | dt13 | 0x70 | LE32 | **方向**：1=主动买（外盘）/ 5=主动卖（内盘） | 决定 dt18 靠近哪个委托号（见下）✅ |
| +16 | dt12 | 0x30 | LE32 | **卖方委托号** | 方向5（主动卖）时 dt18≈dt12，**4183/4183=100%** ✅ |
| +20 | dt74 | 0x30 | LE32 | **买方委托号** | 方向1（主动买）时 dt18≈dt74，**5817/5817=100%** ✅ |
| +24 | dt75 | 0x30 | LE32 | **逐笔序号**（本帧内） | 相邻 +1，**155029/155029=100%** ✅ |
| +28 | dt18 | 0x70 | LE32 | **成交号**（=主动方委托号+小偏移） | 方向1时≈dt74(+3~+9)，方向5时≈dt12(+2~+6) ✅ |

**dt12/dt74/dt18 语义破译（100% 验证）**：

这是深市 Level2 逐笔成交的标准结构——每笔成交对应一次撮合，有买方委托号和卖方委托号。
关键铁证（前 10000 条相邻对验证）：
- **方向=1（主动买/外盘）时**，`dt18`（成交号）≈ `dt74`（买方委托号），差值 ∈ {3,4,8,9...}，
  即成交号紧跟主动方（买方）委托号 → **5817/5817 = 100%**
- **方向=5（主动卖/内盘）时**，`dt18`（成交号）≈ `dt12`（卖方委托号），差值 ∈ {2,3,4,5,6}，
  成交号紧跟主动方（卖方）委托号 → **4183/4183 = 100%**

这完美对应交易所撮合机制：**主动方单子吃掉被动方挂单**，成交号与主动方委托号相邻。
三者末值都≈5434万（从 13:18 起累计），是当时市场的累计委托/成交序号。

**关键发现**：
1. **字段表 dt 编号 ≠ 传统 DataType 语义**：请求 DataType=10,12,13，但响应里
   **dt56 才是价**（非 dt10）、**dt10 是量**（非 dt12）、**dt12 是卖方委托号**。
   逐笔协议的字段映射与分时/K线不同，不能套用 `decode_ths_float` 的 DataType 假设。
2. **fmt 标记不可靠**：dt56 字段表标注 fmt=0x30（整数），但实际存的是 ths_float（价 37.75）。
   → 逐笔帧的字段解码不能只看 fmt，需按 dt 编号单独处理。
3. **深市行主序定长**：虽 flag=0x0046，实测是行主序（非 BitRLE/位平面转置），
   时间戳/价/量直接可读。与 `timeline_protocol.py` 的 0x0046 分支（走 BitRLE）**不兼容**。

**深市 vs 沪市（HANDOFF_SUPERORDER 603118）对照**：

| 维度 | 沪市 603118 | 深市 000938 |
|---|---|---|
| hd 标记 | `hd\x8d1.0` 变体 | 标准 `hd1.0` |
| 帧大小 | 440KB | 2.5MB→4.96MB（正规化后） |
| 字段数 | 29 项 | 8 项 |
| 编码 | 变长/参数化（fmt 子标记 0x11/0x1a 未破） | **行主序定长 32B**（直接可读）|
| 可实现性 | 需逆向 varint | **可直接实现** |

**实现可行性**：深市 7169 定长行主序已完全可读，**8 字段全部破译**，可作为**首个逐笔解析器**。
字段完备（时间戳/价/量/方向/买卖委托号/序号/成交号），足以支撑逐笔成交回放的完整展示。
沪市变长编码留作后续 P2-R&D。

**下一步**：
1. ~~验证 4214 通道（逐笔面板）的 7169 响应是否同构~~ → **✅ 已完成（§J.6，101 帧统一字段表）**
2. 实现深市 7169 解析器（定长行主序），沪市留作后续

## I. 盘口十档实时推送破译准备（2026-08-05 盘后，待盘中抓样本）

> 用户需求：实时推送盘口十档变动（非定时轮询 `depth_quote`）。4214 推送帧的十档字段
> **从未破译过**（现有 `parse_snapshot_push` 只解了价/量/tick号，且 +58 价格偏移在
> 这批帧上解出的值不对）。盘后做不了推送破译（推送帧盘中才产生），本轮只准备抓包工具。

### I.1 现状梳理

- **推送通道已完整接通**：`snapshot_loop` 后台线程（`_client/connection_runtime.py:346`）
  + `snapshot_subscribe`/`latest_price`（`service_facade.py:1149/1223`）+ `__manual` L2
  持久 socket（sh/sz）+ `L2SubscriptionCoordinator.ensure_registered`。71B 帧已被接收。
- **十档字段完全空白**：两个 Explore agent 逐字确认——无文档/测试/代码涉及推送帧的
  十档买卖价量。`parse_snapshot_push` 只解 `{code, market, price(+58), volume(+62),
  tick_seq(+39)}`，且 +58 价格偏移在新样本上解出的值不对（49.52 而非紫光 37.x）。
- **现有十档是查询协议**：`depth_quote`（`quote_protocol.py`）走 4214/4260 请求-响应，
  返回标准 hd1.0 表，字段表 dt24-35（五档）+ dt102-121（六~十档），已完整实现
  （17 测试通过）。但这与 4214 **推送**帧是两个东西，字节格式完全不同。

### I.2 关键发现：推送帧是定长全量快照（非增量）

用现有盘中 pcap（`kanpan_push_20260805_132347.pcap`）的 54 帧 000938 推送测试：
**全部 71B 定长**，帧大小分布 `{71: 54}`。说明推送帧是**定长全量快照**
（每帧含完整十档），不是增量推送（只含变化档位）。这大幅降低破译难度——
不用状态机合并，按固定偏移切字段即可。

### I.3 盘后准备：抓包 + 破译工具

**改动 1：`tests/capture_kanpan_push.py` 加 `--depth-anchor` 模式**

截图对照抓包（无阻塞）。盘中用法：
```
py tests/capture_kanpan_push.py --depth-anchor --code 000938
```
- t=10/30/60s 三个锚点时刻打印截图提示，**不暂停**（截图与抓包并行，±2s 窗口兜住延迟）
- 用户截同花顺十档盘口面板的图，存 `captures_live/depth_shot_<code>_1/2/3.png`
- 75s 后自动结束，收集每个锚点 ±2s 的 snapshot4214 帧
- 存 `depth_push_anchor_<code>_<ts>.json`（帧 hex + 对应截图文件名）
- 0 帧时提示可能原因（非 L2 账号 / 没开十档面板 / 错网卡）

**改动 2：`tests/analyze_depth_push.py`（破译骨架）**

读 JSON + 打印帧概览 + 截图清单 + 帧大小分布。`--reverse` 模式：
把截图十档数字填入 `GROUND_TRUTH` dict，自动反查帧内字节位置（ths_float/LE16/LE32）。

### I.4 明天盘中操作清单（给用户）

1. `py tests/capture_kanpan_push.py --depth-anchor --code 000938`
2. 打开紫光 000938 十档盘口面板（Level2 账号）
3. 看到 ★ 提示就截图（3 次），存 `captures_live/depth_shot_000938_1/2/3.png`
4. 截图发回，用 vision 读十档数字填入 `GROUND_TRUTH`
5. `py tests/analyze_depth_push.py captures_live/depth_push_anchor_*.json --reverse`
6. 根据反查结果归纳字段偏移表，实现推送十档解析

### I.5 待破译的核心问题

> ⚠️ 2026-08-05 盘后 §J.4 修正：以下三条针对的是 **71B 推送帧**（snapshot_loop 通道）。
> **盘口十档的请求-响应查询**（pageid=4214，rc=1，hs=107 fc=23）**早已完整实现**
> （`quote_protocol.parse_depth_quote_response`），字段表 §J.4 已逐字节铁证。
> 不要把「查询十档」和「推送十档」混淆——本节 I.1-I.5 全程说的是**推送帧**。

1. **十档字段偏移**：71B **推送**帧内，买1-5/卖1-5 的价/量在哪些字节？现有 +58 价格偏移
   已知有误，需用截图数字重新定位全部字段。（注：查询帧字段偏移 §J.4 已破，与此无关）
2. **全量确认**：I.2 发现是定长全量，但需截图对照最终确认（万一 71B 不够装 20 个值）。
3. **深沪差异**：000938 是深市，沪市帧结构可能不同（类似 7169 深沪格式差异）。

### I.6 ★ 2026-08-06 盘中抓包验证：71B 推送帧不含十档（推翻 §I.2「全量快照」假设）

> 盘中按 §I.4 清单操作：`capture_kanpan_push.py --depth-anchor --code 000938` +
> 3 张十档截图（36.84–36.94 区间）。pcap =
> `captures_live/depth_push_000938_20260806_131737.pcap`（838KB）。**全程逐字节
> 反查截图价格，结论：pcap 里根本没有十档盘口帧。**

**证据链（三重验证）：**

1. **物理不可能**：十档 = 20 价 + 20 量 = 40 个值，每个最少 2B 也要 80B。71B 去掉
   ~30B 头只剩 ~40B，**装不下 20 个价量对**。§I.2「定长全量快照」的判断**错误**。
2. **截图价格在 pcap 里查无此帧**：截图十档 36.84/36.85/.../36.94，整个 pcap 里
   「6+ 个 36.8–37.0 区间 u16/1000 价格」的帧 = **0 帧**。36.92 作为 u16 只出现 3 次
   且分散，不是盘口序列。
3. **stream0 的 71B 帧是逐笔成交推送**：`+58` 偏移解出的价格分布在 0.0–65.3 全散开
   （78 帧=0.0，峰值在 32.9/41.2 等），是逐笔成交价不是盘口价。stream0 共 1048 个
   000938 帧（67–821B 变长），全是逐笔成交/回放家族。
4. **stream3 的大帧（297–548B）是指数/板块**：代码 `1A0001`（上证）、`1A0002`、
   `1B0680`/`1B0688`，**不含 000938**。这些帧里出现 36.86 等是指数点位附近的巧合。

**根因：`--depth-anchor` 只过滤了 snapshot4214 的 71B 推送帧，但十档实时变动不走这个通道。**
十档要么走请求-响应轮询（`depth_quote` 已实现），要么走另一种更大的推送帧格式（未识别）。

**结论与下一步选择：**

- **「查询十档」**（请求-响应 pageid=4214，rc=1，hs=107 fc=23）→ ✅ **早已完整实现**
  （`quote_protocol.parse_depth_quote_response`，6 测试全绿，fixture 齐全）。定时轮询即可用。
- **「推送十档」**（snapshot_loop 的 71B 帧）→ ❌ **物理上不可能装下十档**，本路径作废。
  若需真正实时推送，须重新抓包抓全 stream0 的变长帧（67–821B），用截图价格反查
  到底哪种帧携带十档——但截图价格（36.8x）与本次 pcap 逐笔价（40–52）严重错位，
  可能抓包时客户端没真正刷新十档面板，需盘中重抓。

### I.7 ★ Level2 指数买卖力量：dt14/dt15（非 dt22/dt23）+ 五大指数活网验证

> 用户需求：Level2 账号实现五大指数（1A0001/399001/399006/1B0680/899050）的
> 买卖力量对比（红绿柱），以普通账号已验证的为真值核对。

**关键纠正：买卖力量的累计字段是 dt14/dt15，不是 dt22/dt23。**

最初按「§I 推送帧字段」的直觉用 dt22/dt23，活网验证发现严重问题：

| 字段 | Level2(0x009e) 非单调点 | 判断 |
|---|---|---|
| dt13(成交累计) | 0/240 | ✓ 单调（BitRLE 解码正确） |
| dt19(成交额累计) | 0/240 | ✓ 单调 |
| **dt14(主动买累计)** | **0/240** | ✓ **严格单调，正确字段** |
| **dt15(主动卖累计)** | **0/240** | ✓ **严格单调，正确字段** |
| dt22 | 116/240 | ✗ 非单调，非累计主动买卖 |
| dt23 | 97/240 | ✗ 非单调，非累计主动买卖 |

- `dt14 + dt15 ≈ dt13`（ths_float 解码后成立，如 rec0: 489839680 ≈ 489839700）
- dt22/dt23 的非单调点是**实时小幅回撤**（最大 -360 万 / 总量级 5.8 亿 = 0.6%），
  是数据特性非解码 bug——但作为「累计值」不达标，不能用于逐分钟差分。
- 字段语义出处：`HANDOFF_PUSH_INVESTIGATION_20260724.md` §513-514 已铁证
  dt14=主动买累计、dt15=主动卖累计。

**改动：`services/timeline.py` `_enrich_buy_sell_force`**
- 字段优先用 `dt14`/`dt15`（单调可靠）；若无则回退 `dt22`/`dt23`
- enrich 触发条件：records 含 (dt14&dt15) 或 (dt22&dt23) 之一

**改动：`features/history_timeline_protocol.py` `NORMAL_HISTORY_TIMELINE_DATATYPE`**
- 末尾追加 `14, 15`。原 DataType 不含主动买卖累计，服务端不返回。
  加 14/15 后响应 flag 从 0x005e 变为 **0x0066**（rs 56→64，多 2 个 4B 字段），
  dt14/dt15 严格单调（0/240），值与 Level2 0x009e 表完全一致（同数据源）。
- **排除了 BitRLE/切分 bug**：dt22/dt23 在 0x005e 和 0x009e 两张表里都非单调
  （111-116/82-96），翻转任何单个 bit 都不能修复（最多改善 1-2 点），
  证明 dt22/dt23 本身就不是累计值，是数据特性。

**改动：`features/timeline_protocol.py`**
- 新增 `NORMAL_TODAY_TIMELINE_FLAG_WITH_FORCE = 0x0066`，
  `TODAY_TIMELINE_FLAGS = {0x005E, 0x0066}`，parser `is_today` 接受两者。

**五大指数活网验证（2026-08-06，Level2 + 普通账号对比）：**

| 指数 | market | n | dt14/dt15 单调 | 全程趋势 | net_force | 红:绿 |
|---|---|---|---|---|---|---|
| 1A0001 上证指数 | 16 | 241 | 0/240 ✓ | ↑+36 | -25 亿（卖强） | 95:144 |
| 399001 深证成指 | 32 | 241 | 0/240 ✓ | ↑+128 | -31 亿（卖强） | 97:142 |
| 399006 创业板指 | 32 | 241 | 0/240 ✓ | ↑+43 | -5.3 亿（卖强） | 112:127 |
| 1B0680 科创综指 | 16 | 241 | 0/240 ✓ | ↑+59 | -4 千万（卖强） | 115:124 |
| 899050 北证50 | 144 | 241 | 0/240 ✓ | ↑+13 | 0（dt14/dt15 全 0） | — |

- **普通账号（加了 14/15 后）与 Level2 结果完全一致**：net_force、红绿比例一字不差。
- 指数涨但 net_force 为负（卖强）是正常的：指数按市值加权（大盘股权重），
  买卖力量按成交量（小票活跃），两者维度不同。

### I.8 ★ 北交所（BSE）分时协议 + 网关发现（2026-08-06 盘后）

> 用户需求：北交所个股（920xxx）和北证50（899050）的分时协议，与沪深不同。

**抓包发现（普通账号 + Level2 双账号 pcap）：北交所用完全不同的 pageid 和 market 码。**

| 标的 | market | pageid | 请求 route | DataType | dt14/dt15 | 买卖力量 |
|---|---|---|---|---|---|---|
| 北交所个股 920xxx | **151** | **10443** | SUB1=0x014a, SUB2=0x0100 | `14,13,19,54,10,23,15,22` | ✓ 含 | **有** |
| 北证50 指数 899050 | **144** | **11695** | SUB1=0x003e, SUB2=0x013e | `272,207,42,271,...` | ✗ 无 | **无**（与客户端一致）|

**网关发现（关键）：北交所数据只在 `main.123ths.com` 的 IP 上可用，`ifindhq` 不支持。**
- `main.123ths.com` → `218.245.102.0` / `8.134.108.168` 等（支持 market 151）
- `ifindhq.123ths.com` → `8.134.123.179` 等（不支持 151，请求超时）
- passport 的 M_hqdns 不含 main.123ths.com（只有 ifindhq），需硬编码补上
- init MarketCode 不需要加 151（加了反而部分 IP 拒绝 init）

**改动：**
- `protocol.py resolve_market_hosts`：优先 `main.123ths.com`（硬编码补入），只在 main DNS 失败时 fallback ifindhq
- `history_timeline_protocol.py`：新增 `build_beijing_timeline_query`（pageid=10443）+ `build_beijing_index_timeline_query`（pageid=11695），双子帧 0x09
- `client.py _market_for_code`：加北交所前缀（43/83/87/920→151，899→144）
- `services/timeline.py`：market=151 / 899 强制走 BASIC+MAIN（不分账号类型）；`select_timeline_plan` 加 code 参数区分 899050
- `timeline_protocol.py`：新增 `BEIJING_TIMELINE_FLAGS = {0x0046, 0x006e}`，parser 放宽 dt40 要求

**活网验证（Level2 账号，盘后）：**
- 920493 北交所个股：241 条，dt14/dt15 单调，买卖力量 net=-306002 红90:绿148 ✓
- 899050 北证50：241 条，dt14/dt15 全 0（无买卖力量，与客户端一致）✓

## J. 超级盘口响应帧全分类破译（2026-08-05 盘后，§H.8 下一步 1 已完成）

> 承接 §H/§I。盘后用户提出「先看超级盘口，虽然复杂但收盘只能做这个」。
> 对 `superorder_20260805_195859_resp_stream1.bin`（13.5MB，558 帧）做**全帧字段表分类**，
> 把请求侧 period（tshark 按 stream 对齐）与响应侧 hd1.0 字段指纹**逐类对应**，
> 多个之前「仅触发时机、无内容证据」的协议获得响应内容铁证。

### J.1 方法：请求 period ↔ 响应字段指纹配对

请求侧用 tshark 按 `tcp.stream` 提取每条流的 `pageid` + `DateTime=NNNN(...)`，
解出 period；响应侧对每个 ≥200B 帧跑 `normalize_8901_response` + hd1.0 头解析，
按 `(hs, fc, 前3字段 dt)` 指纹分组。两者按字段表内容（DataType→字段 dt）天然对应。

### J.2 ★ 请求 period 分布（stream1，含 000938 全程操作）

| period | 次数 | DataType（首例） | 结论 |
|---|---|---|---|
| 7169 | 101 | `10,12,13`（个股逐笔）/ `10,6,66,1111` | **逐笔成交回放**（§H.8 已破，本节验证 4214/4260 同构）|
| 4096 | 25 | `70,69,10,9,8` | **分笔 tick 快照**（hs=27 fc=6，本节破）|
| 7173 | 24 | `10`（恒单价）| 配套短查询（响应字段少，见 J.5）|
| 7174 | 24 | `272,229,14,207,...`（11字段，含 399002 指数）首例 / `10` 其余 | **首例是大查询**，其余配套（修正 §H.3「同 7173 配套」）|
| 7176 | 21 | `10,27,33,49` | 集合竞价（§19 已全绿）|
| 7424 | 19 | `10,49,287`，区间 14:17-15:00 | **收盘集合竞价**（14:57-15:00 段）|
| 8192 | 72 | `10` 等 | 分时 |
| 16384 | 39 | `6`/`19` 等 | 日K（默认加载）|
| 0 | 352 | `7,10,19,6,66`（899050 指数）| pageid=5716 基础订阅 |

### J.3 ★ 响应帧字段指纹分类（19 种指纹，全帧覆盖）

按 `(hs, fc, 前三字段)` 分组，帧数从多到少：

| 指纹 (hs/fc/前3字段) | 帧数 | 对应请求 | 备注 |
|---|---|---|---|
| **hs=32 fc=8 [dt1,dt56,dt10]** | **101** | **7169 逐笔成交** | §H.8 八字段，**字段表统一**（4214/4260 同构，见 J.6）|
| hs=23 fc=2 [dt5,dt127] | 74 | (基础订阅分量) | |
| hs=4 fc=1 [dt56] | 47 | (单字段，疑心跳/序号) | |
| **hs=43 fc=7 [dt5,dt10,dt24]** | 25 | **盘口快照** DataType=`10,24,30,69,70,127` | 最新价/买1/卖1/低/高（J.7）|
| **hs=27 fc=6 [dt5,dt70,dt69]** | 24 | **4096 分笔 tick** | dt70/69/10/9/8（J.8）|
| **hs=107 fc=23 [dt5,dt30,dt31]** | 23 | **十档卖侧** DataType=`...30,31...121` | §quote_protocol 已实现 |
| **hs=107 fc=23 [dt5,dt24,dt25]** | 23 | **十档买侧** DataType=`24,25...119` | §quote_protocol 已实现 |
| hs=39 fc=5 [dt5,dt10,dt85] | 23 | 7173 配套（dt5,10,85,130,6）| 见 J.5 |
| hs=20 fc=5 [dt1,dt10,dt49] | 19 | 7176 竞价 | §19 已实现 |
| hs=27 fc=6 [dt5,dt7,dt10] | 14 | 指数分时（399001）| |
| hs=15 fc=3 [dt5,dt1,dt6] | 13 | 指数快照（399002）| |
| hs=19 fc=3 [dt5,dt1,dt86] | 10 | 单股辅助 | |
| hs=19 fc=4 [dt5,dt1,dt6] | 4 | 指数 | |
| hs=8 fc=2 [dt1,dt6] | 3 | | |
| hs=155 fc=38 / hs=111 fc=27 / hs=191 fc=47 | 各1 | 000938 大快照（多字段合并）| 超级盘口首屏聚合 |
| hs=216 fc=54 [dt1,dt10,dt13] rc=4915 | 1 | **4214 全天分时大帧** | frame[533]，盘后历史帧 |

### J.4 ★★ 盘口十档请求-响应字段表完全破译（推翻 §I.5 推送假设）

**关键认知更正**：§I 设想的「盘口十档推送」与「盘口十档查询」是**两个东西**。
本次证实**十档查询早就有完整实现**（`quote_protocol.py` 的 `build_depth_ten_query` +
`parse_depth_quote_response`，走 pageid=4214 请求-响应，rc=1 单股票快照）。
§I 的 71B 推送帧是另一条通道（snapshot_loop），两者字节格式不同。

**买侧十档字段表**（frame[67]，000938，全 4 字节定长，hs=107 含 15B 尾填充）：

| 记录内偏移 | dt | fmt | 语义 | 实测值 |
|---|---|---|---|---|
| +0 | dt5 | 0x20 | 市场标记/时间 | 3221594872 |
| +4 | dt24 | 0x70 | **买1量** | 321354 |
| +8 | dt25 | 0x70 | **买1价** | 36.93 |
| +12 | dt26 | 0x70 | 买2量 | 207200 |
| +16 | dt27 | 0x70 | 买2价 | 36.92 |
| +20 | dt28 | 0x70 | 买3量 | 64700 |
| +24 | dt29 | 0x70 | 买3价 | 36.89 |
| +28 | dt102 | 0x70 | 买4量 | 24300 |
| +32 | dt103 | 0x70 | 买4价 | 36.88 |
| +36 | dt106 | 0x70 | 买5量 | 272200 |
| +40 | dt107 | 0x70 | 买5价 | 36.87 |
| +44 | dt110 | 0x70 | 买6量 | 9500 |
| +48 | dt111 | 0x70 | 买6价 | 36.86 |
| +52 | dt114 | 0x70 | 买7量 | 30600 |
| +56 | dt115 | 0x70 | 买7价 | 36.85 |
| +60 | dt118 | 0x70 | 买8量 | 137300 |
| +64 | dt119 | 0x70 | 买8价 | **0x20302045 空格填充=无效** |
| +68 | dt127 | 0x20 | 标记 | 空格填充 |
| +72 | dt150 | 0x70 | ? | 0 |
| +76 | dt151 | 0x70 | ? | 0 |
| +80 | dt154 | 0x70 | 买?价 | 36.91 |
| +84 | dt155 | 0x70 | 量 | 135200 |
| +88 | dt6 | 0x70 | 昨收 | 36.90 |

**卖侧**（frame[65]）：dt30=卖1量/dt31=卖1价=36.96 → dt121=卖8价=37.04，结构对称。

**铁证**：
- 价格单调：买侧 dt25→dt115 = 36.93→36.85 严格递减；卖侧 dt31→dt117 = 36.96→37.04 严格递增（dt119/dt121 空格填充需过滤）
- 字段表 23 买帧 + 23 卖帧**完全稳定**（无变体）
- 买卖侧 dt5 恒差 -100（同时刻配对，编码差异）
- 与现有 `BUY_LEVEL_FIELDS`/`SELL_LEVEL_FIELDS` 常量**逐字段吻合** → 现有解析器已正确

**结论**：盘口十档查询**无需新增解析器**，`parse_depth_quote_response` 已覆盖。
真正未做的是 §I 的**推送**十档（71B 帧），那需要盘中抓样本（§I.4 流程不变）。

### J.5 7173/7174 响应内容（首次获得，修正 §H.3）

- **7173**（DataType=10 恒单价）：响应疑为 hs=39 fc=5 `[dt5,dt10,dt85,dt130,dt6]`，
  rc=1，字段多为 `0xFFFFFFFF` 哨兵（盘后历史查询单价无意义）。**确认是配套短查询**，
  非独立业务。§H.3「配套短查询」判断**成立**。
- **7174 首例**：DataType=`272,229,14,207,271,228,13,227,19,40,226`（11 字段，含 399002 指数），
  这是**多字段大查询**（不是简单配套）。其余 7174 是 DataType=10 配套。
  §H.3「7174 同 7173 配套」**部分修正**：首例 7174 是独立大查询，语义待响应破译。

### J.6 ★ §H.8 下一步 1 已完成：4214/4260 通道 7169 同构

101 个 hs=32 fc=8 帧**字段表完全统一**（仅一种 `[dt1,dt56,dt10,dt13,dt12,dt74,dt75,dt18]`），
rc 从 34（4214 翻页小段）到 155031（4260 超级盘口全天）不等。
**结论：4214（逐笔面板）和 4260（超级盘口）共用同一 7169 解析器**，无需分别实现。
§H.8 字段布局对两通道均成立（DATA_OFF=201，行主序定长 32B，八字段全铁证）。

### J.6bis ★★★ 沪市 7169 已破：normalize 后 = 深市同构（推翻 HANDOFF_SUPERORDER §9-§16）

> 用户新抓沪市 603118 超级盘口（`superorder_20260805_234035.pcap`，4.8MB）。
> **决定性发现：沪市 7169 的「变长」根本不是内层变长，和 7176 竞价一样是外层
> cmd=0x0a 压缩——过了 `normalize_8901_response` 后，沪市 7169 = 深市 7169 = 定长行主序 32B。**
> HANDOFF_SUPERORDER §9-§16 花海量篇幅纠结的「fmt 子标记 0x11/0x88 / x32dbg 逆向 /
> 参数化变长编码」**全部基于未 normalize 的原始字节**，方向错误。
> （normalize_8901_response 是该文档 §17 才补的，补完后没人回头验证 7169。）

**铁证（frame[36]，沪市 603118，原 634KB `hd\x8d1.0` 变长帧）**：

```
normalize_8901_response(frame36):
  raw 634,778 B (hd\x8d1.0)  →  normalized 1,206,822 B (标准 hd1.0\0)
  hd1.0 头: rc=37707 flag=0x0046 hs=32 fc=8
  字段表: dt1(0x30) dt56(0x30) dt10(0x70) dt13(0x70) dt12(0x30) dt74(0x30) dt75(0x30) dt18(0x70)
  ↑ 与深市 000938 字段表【逐字段完全一致】
```

**沪市字段布局**（DATA_OFF=202，shell=`0x11+'603118'`@180，shell→data 间隔 22B；深市 DATA_OFF=201，shell=`0x21+'000938'`@179，间隔同为 22B）：

| 记录内偏移 | dt | 语义 | 沪市实测（603118 13:19:59）| 深市实测（000938 13:18:51）|
|---|---|---|---|---|
| +0 | dt1 | 时间戳 | 1785907199 ✓ | 1785907131 ✓ |
| +4 | dt56 | **成交价** | 14.57 ✓ | 37.75 ✓ |
| +8 | dt10 | **成交量**（手）| 1600 ✓ | 100 ✓ |
| +12 | dt13 | **方向**（1主动买/5主动卖）| 1 ✓ | 5 ✓ |
| +16 | dt12 | 委托号 A | 17754168 | 37048499 |
| +20 | dt74 | 委托号 B | 17762089 | 37045605 |
| +24 | dt75 | **逐笔序号**（严格+1）| 139727 ✓ | 497839 ✓ |
| +28 | dt18 | 成交/委托序号 | 29100386 | 37048731 |

**沪深差异（重要，dt12/dt74/dt18 语义不同）**：
- **深市**：dt12=**卖方**委托号，dt74=**买方**委托号，dt18=成交号(≈主动方委托号+小偏移)
  - 铁证：dir=1 时 dt18≈dt74（5817/5817=100%），dir=5 时 dt18≈dt12（4183/4183=100%）
- **沪市**：dt12=**主动方**委托号（dir=1/5 都随每笔变化），dt74=**被动方**挂单号（多笔共用），dt18=独立成交序号（量级与委托号差 1200 万，非相邻）
  - 铁证：dir=1 块里 dt74 恒定/dt12 变（22675 相邻对），dir=5 块同理（10632）
  - 沪市 dt12/dt74 量级重叠（1739-1848 万，同序列），**不能靠量级区分买卖**，靠「变化频率」区分主被动

**验证规模**（stream-wide，严格 ts 过滤 9:00-15:30）：
- 沪市 603118：**70742 条**好记录，价格 100% 合理（14.x 元），方向 99.99% 是 {1,5}，seq 单调 99.8%
- ⚠ 注意：单帧 biglen 常大于实际抓到长度（TCP 截断），解 record 时**必须用「ts 合法 + stride=32」过滤**，否则尾部混入下帧头部（CodeList 文本）产生乱码。rc 字段在截断帧上不可信。

**结论**：**沪深 7169 共用同一解析器骨架**（normalize → 找 hd1.0 → shell → DATA_OFF → 行主序 32B 切记录）。
仅 dt12/dt74/dt18 三个字段语义需按市场分支（深市=买/卖委托+成交号，沪市=主动/被动委托+成交序号）。
价/量/方向/时间戳/逐笔序号五字段沪深语义一致。**HANDOFF_SUPERORDER 的沪市 7169 逆向工作作废。**

### J.6ter ★ 服务端响应模式：一个区间请求 → 一个响应帧（2026-08-06 核实）

用户提问「打开超级盘口时服务端是否一次性发完全部盘口数据」。对深市 000938 抓包
（`superorder_20260805_195859`）逐帧核实，结论：

**一个 DateTime 区间请求 → 一个响应帧**，服务端按请求区间一次性返回该段全部逐笔。

| frame | 请求区间 | 响应帧大小 | 记录数 |
|---|---|---|---|
| 536 | 13:18:51-15:00:00（101 分钟）| 2.5MB | 155030 |
| 551 | 10:10:30-10:15:24（5 分钟）| 782KB | 49490 |
| 550 | 10:15:24-10:20:30（5 分钟）| 740KB | 46038 |
| 549 | 10:20:30-10:26:51（6 分钟）| 427KB | 26156 |

**关键观察**：
1. 每帧覆盖一个连续时间段，相邻帧区间精确衔接（550 止 = 549 起）——服务端按时间段分页
2. **不是分多帧**：frame 536 一个 2.5MB 帧含 101 分钟 15.5 万条逐笔（TCP 上拆成多包，由 read_frame 重组）
3. **不是一次性全天**：客户端分别请求不同区间（超级盘口滚动时按可视段请求），每段一帧
4. 之前「75 万条重复」根因：用户反复滚动同段时间（76 个高度重叠请求），每请求都回完整区间，
   parser 跨帧累加未去重 → 已在 SuperorderService 加 seq 去重修复

**对实现的影响**：
- `SuperorderService.max_frames=32` 是兜底（防异常分页），正常只读 1 帧
- 单帧可达 2.5MB，`timeout` 需足够（默认 12s 读 2.5MB 在宽带下 OK，慢网可调大）
- 收盘集合竞价段（14:57-15:00）所有逐笔打 15:00:00 时间戳（frame 71 dc=368 全是 15:00:00）

### J.7 盘口快照帧（hs=43 fc=7，DataType=10,24,30,69,70,127）

frame[39]（rc=1）字段：`dt5(标记) dt10(最新价=36.94) dt24(买1价=36.95) dt30(卖1价=37.75)
dt69(最低=30.89) dt70(最高=空格填充) dt127(标记)`。这是**pageid=1334/4214 的盘口快照查询**
（DataType=`10,24,30,69,70,127`），对应同花顺分时图侧栏的五档摘要。dt70 此处空格填充
（盘后无最高价），盘中应有值。

### J.8 4096 分笔 tick（hs=27 fc=6，DataType=70,69,10,9,8）

frame[43]（rc=1）字段表 `[dt5,dt70,dt69,dt10,dt9,dt8]`：
- dt70=37.75（高?）、dt69=36.94（低?）、dt10=34.59（异常，疑开盘/昨收）、dt9=37.75、dt8=异常
- 尾部 3 字节 `4b 01 12`（hs=27 但 6×4=24，多 3 字节，语义待定）
- §H.3「4096 分笔 tick」类比**部分成立**，但字段语义（dt70/69/10/9/8）需盘中数据对照

### J.8bis ★★★ 4096 双形态真相 + 超级盘口分时线破译（2026-08-06 盘后）

> 用户反馈「打开超级盘口看到曲线立马停了」「3 秒一个盘口」，新抓包
> `superorder_20260806_005816.pcap`（冷启动直进超级盘口）证实：
> **§J.8 描述的只是 4214 通道的 4096（小快照）；4260 通道的 4096 是完全不同的
> 东西——全天「每 3 秒一个盘口快照」的回放，这就是超级盘口分时曲线的数据源。**

**4096 双形态（两通道字段表天差地别）**：

| 通道 | 字段表 | 业务 | 帧 |
|---|---|---|---|
| **4214**（§J.8）| hs=27 fc=6 `[dt5,dt70,dt69,dt10,dt9,dt8]` rc=1 | 单点小快照（最新高/低/价?）| 24 帧 |
| **4260**（本节）| hs=216 fc=54 `[dt1,dt10,dt13,dt12,dt20,dt21,dt49,dt18,dt19,dt74,dt75,+完整十档dt24-125]` rc=4914 | **全天 3 秒盘口回放** | 1 大帧 |

**4260 通道 4096 响应（frame[11]）实测**：
```
raw 463,444B → normalize 1,062,313B
hd1.0 @ 98, flag=0x00FE, hs=216, fc=54, rc=4914
时间点 4766 个，跨度 340 分钟（9:15-15:00 全天）
均匀间隔约 4.3 秒/点（集合竞价段稀疏稀释，连续竞价段约 3 秒/点）
```

- flag=0x00FE 走 **BitRLE 位平面**编码（非行主序，需 `_decode_bitrle_0x13746d0` +
  `_transpose_bitplane_0x1763410`，与 timeline 同族）
- 字段含**完整十档**（dt24-29 买 + dt30-35 卖 + dt102-125 六~十档）+ dt10(价)/dt13(量)/
  dt19(额)/dt20(?)dt21(?)dt49(?)——每 3 秒一个完整盘口切片
- 用户看到的「曲线立马出现」= 这帧一次性返回全天所有 3 秒盘口点，客户端连线即得曲线

**分时线真相（推翻前几轮的寻找）**：
- 超级盘口的分时曲线**不是逐笔(7169)聚合**，也**不是普通分时(8192)**——打开超级盘口时
  **根本不发 8192**（全程 8192 只查 399002 指数，无个股）
- 它是 **4260 通道的 4096**：每 3 秒一个盘口快照，全天约 4766 点，取每点的 dt10(最新价)
  连线即得分时曲线；同时每点带完整十档，可做「盘口回放」（拖动时间轴看任意时刻盘口）
- 与普通分时图（`client.timeline`，period=8192，每分钟一个 OHLC 点）是**两套不同数据**：
  普通分时=分钟级 OHLC，超级盘口分时=3 秒级盘口快照序列

**实现状态**：4260 通道 4096 **未实现**。它是 BitRLE 编码（flag=0x00FE），解析路径与
7169（行主序）完全不同，需复用 timeline 的 BitRLE 解码器。这是超级盘口「另一半」核心
功能（7169 逐笔 + 4096 盘口回放），优先级高。

**dt5 时间戳编码**（4260 通道特有，非标准 unix 秒）：
- frame[11] 的 dt5 值如 3221571672，差值 400/3800 等——疑为毫秒级 MarketTime 编码
  （3800ms≈4 秒，吻合 3 秒节拍）。精确解码待 BitRLE 解出后对照确认。

### J.8ter ★ 盘后/历史超级盘口 = 4417@4096（2026-08-07 盘后抓包）

> 用户复测「东山精密昨天超级盘口价格不对」，新抓包 `superorder_20260807_160102.pcap`
> （16:01 盘后）确认：**盘后/历史场景客户端不走 4260，改用 pageid=4417**。

**请求（单子帧，字节级样本 `tests/fixtures/superorder/req_002384_4417_p4096.bin`）**：

```
09 00 16 00 00 <seq> 12 00 09 00 7d 01 ...
CodeList=33(002384,);
DataType=7,10,12,13,18,19,20,21,25,26,27,28,29,31,32,33,34,35,49,75,123,125,
         150,151,152,153,154,155,156,157,6,66,1110,
DateTime=4096(0-0) / 4096(<abs>-<abs>) / 4096(-N-0)
LackTime=0,0,0,0,0,0,0,0
pageid=4417
```

**响应**：hd1.0 **flag=0x009E hs=120 fc=30**，行主序定长（非 BitRLE），
dc=4920（当日）或 0x040012E2 低 16 位（历史，如 4 月 10 日 4834 点），
dt1=unix 秒（约 3 秒/点）、dt10=最新价、字段含买卖档位 dt25-35 / dt150-157 / dt123/125，
**无 102-125 六~十档**（与 4260 的 0xFE/216/54 布局不同）。
历史嵌套帧壳后有 0xFF 填充，行起点需按「连续 ≥2 条合法 unix 秒」定位。

**实现**（2026-08-07）：`build_snapshot_replay_query` 支持 pageid=4417 单子帧形态
（与抓包字节级一致）；`parse_snapshot_replay_response` 同时接受 0xFE/0x9E 两种布局；
`client.snapshot_replay(code, start=, end=)` 传区间时自动走 4417。

**活网验证**：002384 昨日（08-06）全天 4905 点，价格 **179.00-195.34**（09:15-15:30），
解析正确；当日 0-0 走 4260 仍正常（4920 点 187.68-202.10）。

### J.8quater ★ 指数历史超级盘口 = pageid=77 + 双查询对（2026-08-07 盘后抓包）

> 用户抓五大指数历史超级盘口（`superorder_20260807_163545.pcap`）。发现**指数不走
> 4417，而是 pageid=77**（指数历史通道），且请求是**一帧双查询对**：

```
对1: outer 0x0002/0x0038 (CodeList + pageid=77) + inner 0x0009/0x0138
     (DataType=7,10,12,13,...,157,6,66,1110 + DateTime=4096(0-0) + pageid=77)
对2: 同构 + DateTime=4096(<请求区间>)   ← 第二对内层 seq=0，length=文本+1
```

**响应**：一帧含**两张 hd1.0 表**（今日 0-0 + 目标日），布局
**flag=0x0046 hs=32 fc=8 字段 [dt1,dt10,dt13,dt19,dt49,dt18,dt123,dt125]**
（与 7169 逐笔同 flag/hs/fc，靠 dt10@4 区分；与个股 4417 的 0x9E/120/30 也不同）。
北证50（899050）**无历史超级盘口**（客户端不请求 4096）。

**实现**（2026-08-07）：`build_snapshot_replay_query` 支持 pageid=77 双查询对
（字节级对齐 fr542）；`parse_snapshot_replay_response` 遍历一帧内全部 hd1.0 表，
接受 0xFE/0x9E/0x46 三种布局；`client.snapshot_replay(指数, start=, end=)` 自动走
77 并在服务层按请求区间过滤出目标日。

**活网验证（08-06 昨日）**：
| 指数 | 点数 | 点位区间 |
|---|---|---|
| 1A0001 上证指数 | 5218 | 3864.27-3902.05 |
| 1B0680 科创综指 | 5216 | 1894.23-1977.92 |
| 399001 深证成指 | 4745 | 13922.41-14252.14 |
| 399006 创业板指 | 4744 | 3444.36-3574.41 |

另：本抓包同时证实**指数历史分时响应含 dt40**（flag=0x42 hs=28 fc=7 字段
[1,10,13,19,22,23,40]），`client.history_timeline` 已支持黄线还原（`lead_price`）。

### J.8quinquies ★ 北证50（899050）历史分时 = hd3.1 BitRLE（2026-08-07 补）

> 用户确认北证50 历史分时有黄线。抓包核实：899050 历史分时请求走
> **pageid=5703** + `8192(bar区间)` + `DataType=13,19,40,10,23,22,6`（含 dt40），
> 与沪/深指数的 pageid=77 不同；响应是 **hd3.1 flag=0x42 hs=28 fc=7** 的
> **BitRLE 位平面表**（壳 26B，marker 0x90 + 899050，BE expected_size=count×hs），
> 不是 1A0001 等指数的 hd1.0 行主序。

**实现**：`parse_history_timeline_response` 新增 hd3.1 BitRLE 分支（壳 26B +
expected_size 校验 + `_decode_bitrle`/`_transpose_bitplane`），行 bar 有固定缺口
（241 个有效点，非全分钟连续），只保留合法 bar 行；`client.history_timeline("899050",
date=...)` 自动走 5703。活网验证 08-06：241 点，点位 1109.50-1126.01，
dt40/lead_price 齐全（昨收 1119.46 自动还原）。

至此**五大指数历史分时黄线全部打通**（1A0001/1B0680/399001/399006 行主序 +
899050 BitRLE）。

### J.9 今晚可落地的实现候选（按把握排序）

1. **沪深通用 7169 逐笔成交解析器**（§H.8 + §J.6 + §J.6bis 三重确认，**最高把握**）：
   - 沪深都走 `normalize_8901_response` → 标准 hd1.0 → 行主序定长 32B
   - 八字段全铁证，4214/4260 两通道同构，沪深同骨架

### J.9 今晚可落地的实现候选（按把握排序）

1. **沪深通用 7169 逐笔成交解析器**（§H.8 + §J.6 + §J.6bis 三重确认，**最高把握**）：
   - 沪深都走 `normalize_8901_response` → 标准 hd1.0 → 行主序定长 32B
   - 八字段全铁证，4214/4260 两通道同构，沪深同骨架
   - 仅 dt12/dt74/dt18 三字段语义按市场分支（见 §J.6bis 差异表）
   - **不再需要 x32dbg 逆向沪市**（HANDOFF_SUPERORDER §9-§16 作废）
   - ✅ **已实现 + 活网验证通过**（§J.10bis）
2. **盘口十档查询**：**已实现**（`parse_depth_quote_response`），本次仅验证字段表正确，无需改动
3. **★ 4260 通道 4096 盘口回放**（§J.8bis，**下一个高优先级**）：
   - 每 3 秒一个完整盘口快照（含十档），全天 ~4766 点
   - flag=0x00FE 走 BitRLE 位平面，需复用 timeline 的 `_decode_bitrle_0x13746d0`
   - 这是超级盘口分时曲线的数据源（用户证实「3 秒一个盘口」）
   - 4214 通道 4096（§J.8 小快照）是另一个东西，别混
4. 7174 大查询：响应内容未破译，优先级低

### J.10 今晚的认知修正（给后续会话）

| 旧认知 | 修正 |
|---|---|
| §I「盘口十档完全未破译」 | **十档查询早实现**（quote_protocol）；未破的是 71B **推送**帧 |
| §H.3「7174 同 7173 配套」 | 首例 7174 是 11 字段大查询（含指数），非配套 |
| §H.8「待验证 4214/4260 同构」 | **已验证同构**（101 帧深市 + 沪市统一字段表）|
| §H.3「4096 双形态」 | 4214 通道是 dt70,69,10,9,8（hs=27 fc=6）；4260 形态本样本未单独出现 |
| **HANDOFF_SUPERORDER §9-§16「沪市 7169 变长需 x32dbg 逆向」** | **★★★ 作废**。沪市 7169 经 normalize 后 = 深市同构（定长 32B 行主序）。卡的是外层压缩，不是内层变长 |
| §J.6bis 前的「沪市 dt12/dt74 = 买/卖委托」假设 | 沪市 dt12=主动方委托、dt74=被动方挂单（与深市买/卖划分不同，靠变化频率区分）|
| §J.8「4096 = 分笔 tick hs=27 fc=6」 | **只是 4214 通道的 4096**。4260 通道的 4096 是 hs=216 fc=54 全天 3 秒盘口回放（§J.8bis），两者字段表天差地别 |
| 「超级盘口分时线来源未知」 | **4260 通道 period=4096**：每 3 秒一个盘口快照，全天 ~4766 点。不是逐笔(7169)聚合，不是普通分时(8192)。用户证实「3 秒一个盘口」「曲线立马出现」 |

### J.10bis ★★★ 7169 解析器已实现 + 活网验证通过（2026-08-06）

> 承接 §J.6bis/§J.6ter。沪深通用 7169 逐笔成交回放解析器已落地为生产代码，
> 活网实测沪深两市均通过。

**新增文件**：
- `src/thspypc/features/superorder_protocol.py` — `build_superorder_query` + `parse_superorder_response`
- `src/thspypc/services/superorder.py` — `SuperorderService.superorder`（L2 连接 + 4214 注册 + 7169 请求）
- `tests/test_superorder_protocol.py` — 12 个离线测试（合成帧 + 深沪 captures_live 回归 + 请求逐字节匹配 fixture + reexport）
- `tests/fixtures/superorder/req_000938_4214_p7169.bin` — 真实请求 body（逐字节回归基准）
- `tests/verify_superorder_online.py` — 活网验证脚本（可复用回归）
- `client.py` / `service_facade.py` / `services/__init__.py` / `protocol.py` — 装配 + reexport

**用法**：
```python
from datetime import datetime
with THSClient(user, pwd) as client:
    ticks = client.superorder("000938", datetime(2026,8,5,14,30), datetime(2026,8,5,15,0))
    # 每条: {time, price, volume, direction(1买/5卖), delegate_a, delegate_b, seq, trade_no, ...}
```

**请求构造关键修正**：7169 请求是**三层嵌套**（外层 route=0x02fc → 内层1 route=0x02e2
→ 内层2 route=0x01fc），不是单层。初版写成单层 + route 误用 0x01fc，被「逐字节对比真实抓包」
发现（`test_build_superorder_query_matches_capture`）。fixture 是真实请求 body，回归保护。

**活网验证结果**（2026-08-06 凌晨查 8-5 尾盘 14:30-15:00）：

| 股票 | 条数 | seq 连续度 | 首笔 | 末笔 | 价区间 | 方向 |
|---|---|---|---|---|---|---|
| 深市 000938 | 48858 | 48857/48857 (100%) | 14:30:00/37.03 | 15:00:00/36.94 | 36.85-37.08 | 买26816+卖22042(全{1,5}) |
| 沪市 603118 | 12010 | 12009/12009 (100%) | 14:30:00/14.36 | 14:56:59/14.41 | 14.30-14.41 | 买5791+卖6219(全{1,5}) |

- seq 100% 严格连续（单次请求无缺失），证明 parser 解对每一笔
- 方向全 {1,5} 零噪声——证实 §J.6ter「抓包噪声是跨帧重复造成，单次请求无此问题」
- 深市末笔 15:00:00/36.94 = 收盘价；沪市末笔 14:56:59（14:57 后无逐笔）

### J.10ter ★ 逐笔成交 ≠ 每 3 秒一笔（澄清易混点）

用户提问「逐笔之间隔 3 秒吗」。**不是**。3 秒节拍是**集合竞价 7176**独有
（9:15-9:25 每 3 秒一次虚拟撮合，ts 严格 3 秒网格，文档 HANDOFF_SUPERORDER 的
「ts%3 过滤」即为此设计）。**逐笔成交 7169 是每笔一条，无固定间隔**，密度由市场活跃度决定。

实测 000938 尾盘半小时 48858 条：同一秒内 1-几十笔不等（峰值一秒十几笔），平均每秒约 27 笔。
前 20 笔全在 14:30:00 同一秒（相邻 ts 差=0）。这恰恰说明 7169 是真实连续竞价撮合记录，
不是定速采样。

### J.10quater ★ 同花顺逐笔面板 = 按主动方委托聚合（现有字段可还原）

用户截图展示同花顺「逐笔成交」面板：每行不是单笔成交，而是**按主动方委托号聚合**
（一个大买单吃掉多笔小卖单 → 显示成一行，汇总手数/金额/吃掉笔数）。

**现有字段完全够还原**（活网验证）：

| 同花顺列 | 数据来源 |
|---|---|
| 时间 | 首笔 `time`（后续行省略是 UI 渲染，非数据字段） |
| 价格 | `price` |
| 金额 | `Σ(volume × price × 100)`（手→股×价） |
| 主动方 | `direction`（1=主买/红，5=主卖/绿） |
| 买单/卖单数量 | dir=1→主动买方 `Σvolume`；dir=5→主动卖方 `Σvolume`；吃掉的对手笔数 = 该委托成交笔数 |

实测验证（000938 尾盘）：48858 笔按主动方委托聚合 → 23940 个委托（平均 2.04 笔/委托）。
典型「大单吃多笔」：14:39:10 一个主动买单（333300 手）吃掉 **425 个**不同被动卖方委托。

**沪深聚合 key 差异**（§J.6bis 字段语义差异的延伸）：
- 深市：`dir=1`→主动方=`delegate_b`(买方)，`dir=5`→主动方=`delegate_a`(卖方)
- 沪市：`delegate_a` 始终是主动方（无论方向）

聚合逻辑属展示层，未入协议模块；如需可加 `aggregate_ticks()` 工具函数（按市场分支选 key）。

### J.11 本节探查脚本（临时，未入库）

- `/tmp/classify_resp.py` — tshark 按 stream 提取 period 分布
- `/tmp/scan_frames.py` / `/tmp/deep_scan.py` — 全帧 hd1.0 字段指纹分类
- `/tmp/decode_tenlevel.py` / `/tmp/verify_tenlevel.py` / `/tmp/final_tenlevel.py` — 十档字段逐字节锁定
- `/tmp/7169_channels.py` — 4214/4260 通道字段表同构验证

这些是今晚一次性探查脚本，结论已固化在本节；若要实现 7169 解析器，
参考 §H.8 字段布局表 + 本节 J.6 同构结论即可，无需重跑探查脚本。

### J.12 ★★★ 7173 / 7174 = 买一 / 卖一委托队列（2026-08-09）

后续用历史超级盘口请求和客户端截图完成闭环，覆盖并修正本文件 §H.3 / §J.5 的
“配套短查询、业务待定”旧结论：

- 7173 = 买一委托队列，7174 = 卖一委托队列，二者均为 Level2 专属；
- 请求为 `DataType=10, DateTime=<period>(-1-0)`；
- 响应为 `hd1.0, flag=0x002A, hs=4, fc=1, dt56`；
- 队列原始值低 27 位为股数，`0x08000000` 为主力单高亮位；
- `total_order_count` 是价位总笔数，响应最多给约 50 个可见委托；盘口总股数应从
  4096 的 dt25（买一）/dt31（卖一）取得；
- 空侧仅返回 ACK，属于合法空队列；
- 历史日必须先在**同一条已登录市场 L2 socket** 请求 4096 建立 4417 上下文，
  再发 7173/7174。当前只确认最新一个交易日有数据。

截图铁证：688693 买队列的 4 个主力单合计 1230.93 手，对齐 UI 的 1231 手/4 笔；
000779 卖队列的 3 个主力单合计 12588.98 手，对齐 UI 的 1.3 万手/3 笔。
生产实现见 `src/thspypc/features/superorder_protocol.py`、
`src/thspypc/services/superorder.py` 和 `docs/guides/ORDER_QUEUE.md`。

2026-08-09 生产 API 活网复验：688693 返回买一 93.38 / 700 笔 / 可见 49 / 主力
4 笔 1230.93 手 / 总量 10698 手，卖一空；000779 返回买一空、卖一 12.85 /
7756 笔 / 可见 49 / 主力 3 笔 12588.98 手 / 总量 145824 手。普通账号在未建立
L2 socket 前正确拒绝。
